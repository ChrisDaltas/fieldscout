# SE Task Breakdown — Custom Scoring Editor (2026 test cohort, spec §7.3.3.1)

> **Architect session output — 2026-08-18.** Authorized by Chris's approval of PR #152 (spec **v2.11**, merged as `7061460`): §7.3.3.1 is LAW, and its own text says *"Build is NOT authorized by this section alone: a follow-up Architect task breakdown (its own doc, after Chris approves this entry) cuts the tasks."* This is that doc. ~~**This PR — #161 — stays OPEN for Chris: breakdowns are his approval gate (the #150/#155 precedent); the loop takes `SE.*` tasks per `ACTIVE-BUILD.md` only after he merges it.**~~ **[corrected 2026-08-25 — the gate is DISCHARGED, not live.** `gh pr view 161 --json state,mergedAt,mergeCommit` → `MERGED 2026-08-19T16:40:38Z`, mergeCommit `b2b1927`. `SE.*` is takeable now; no approval clause in this doc is still pending.**]**
>
> ### ⚠ AMENDED 2026-08-25 — READ §0 BEFORE ANY SE TASK
>
> This breakdown was authored 2026-08-18 against `main` @ `7061460` and never revised. **Five lanes landed on top of it** (AP, MP, MS, and M3's close-out): migrations went **087 → 102**, pgTAP **036 → 050**, the spec **v2.11 → v2.16.6** (2424 → 2552 lines). A docs-only amendment pass re-measured every claim in this file against `main` @ `6b1e4e5` and corrected them **in place, marked, with the measuring command** — nothing was silently rewritten. **§0 is the index of what moved.** Two corrections are not staleness and change what gets BUILT: **§0(A)** gives SE.2 a sixth deliverable without which the auction room's projections go blank, and **§0(B)** retires a hook SE.6 planned to write because it already ships. Decision **D266**.
>
> **What SE is:** the C37 **separate parallel track** — never an M3 lane (§7.3.3.1's own charter sentence). It puts a per-position custom scoring editor in front of the untouched D33/D57 dot-product calculator for the invite-only 2026 test cohort: a format-2 rules envelope + pure resolver, the F21 guardrails as server-side validation law, fork-from-template entry, and a six-sample-player live editor UI. Templates stay format 1 and byte-identical; **zero scored-outcome change for existing template leagues** is the track's headline property and it is property-pinned, not asserted. **Enforcement posture (Chris, ruled in-session 2026-08-18: "creating an invalid scoring system should not be possible"):** an invalid league scoring document is **unrepresentable at the table itself** — a write-time trigger on `scoring_systems` (D175) — with the editor-save/attach/draft-start checks as layered defenses carrying the user-facing errors.
>
> **What SE is NOT:** boundary editing (per-league `tier_cuts` inputs — **F59**, routed to a follow-up breakdown AFTER this track; SE.8 pins that no boundary input exists) · IDP, bonuses, TE premium (`scoring_surface: 'reserved'`) · sharing/marketplace of custom systems · post-draft scoring edits (the existing commissioner-override law, not exposed here) · the "same game, scored three ways" widget (v2.7 deferral stands) · `return_yards` (ships code-gated OFF until its Q13 data task — §8 note below) · any `is_pro` gate (free for everyone — Pro suspension, CLAUDE.md 2026-08-03).

---

## 0. Amendment — re-measured 2026-08-25 against `main` @ `6b1e4e5` (docs-only; **D266**)

> **Why this section exists.** This document was written once, on 2026-08-18, and was never revised while five lanes landed under it. Every number in it that pointed at a moving target — migration heads, pgTAP heads, spec line numbers, function bodies, "already-built" surveys — had drifted by 2026-08-25, and two of the drifts had crossed from *stale* into *wrong*. **The generalisable rule, recorded as D266: a breakdown that outlives the lanes it was written beside is not a plan any more until it has been re-measured. Re-measure before you build, not after the review finds it.** The doc's own §4 rule 9 already said the mechanism (cite by symbol, not by line); this amendment applies it where it can, because this file has now drifted once and will drift again.
>
> **House convention used throughout:** every correction is made **in place and marked** — the original claim is kept (struck through or quoted), tagged `[corrected 2026-08-25]`, and given the command that measured the replacement. A future reader inherits the correction *and* the reason it was needed.

### A. THE ONE THAT CHANGES THE BUILD — SE's format-2 envelope would blind the auction room's projections, and no SE task owned it

**Nothing in §1's survey was wrong; the survey asked the wrong question.** §1's last bullet establishes that nothing in `src/` calls `scorePlayerWeek` against a league snapshot in a production path, and that is still literally true (`grep -rn "scorePlayerWeek" src/ | grep -v '\.test\.'` → its own definition at `calculator.ts:66`, nothing else). But a **different** production path reads the league's rules document and interprets a **top-level** key:

```
src/components/draft/auction-player-table-ops.ts  →  export function scoringFamilyFromRules(rules)
  const perReception = (rules as Record<string, unknown>).receptions          // the top-level read
  if (typeof perReception !== 'number' || !Number.isFinite(perReception)) return null
```

SE.2(1)'s format-2 envelope is `{ format: 2, base: {…receptions…}, positions, tier_cuts }` — **there is no top-level `receptions`**. Run against the shipped function: format 1 → `'ppr'`; format 2 → `null`.

The consequence chain, each link measured:

| Link | Site (symbol, per §4 rule 9) | Effect |
|---|---|---|
| family `null` | `projectedPointsFor` (`auction-player-table-ops.ts`) — `if (family === null) return null` | projected points null |
| null projection | `decorateRows` — `projectedPoints` / `pointsPerWeek(…)` / `dollarsPerPoint(…)` | **three** columns null: Proj, Pts/wk, **and $/pt** |
| all three null | `auction-player-table.tsx` (`grep -n "Scoring not readable" src/` → `auction-player-table.tsx:531`) | the room renders **"Scoring not readable — projections hidden"** |

**Both arms of the reading hook are blind to it.** `useLeagueScoringFamily` returns `leagues.scoring_rules_snapshot` when present — and SE.4b(5) pins that snapshot as the format-2 doc **verbatim** (`059_scoring_snapshot.sql:235` is a straight `SET scoring_rules_snapshot = v_rules`) — and otherwise reads `scoring_systems.rules` directly. Neither arm unwraps an envelope.

**And SE.10's gate would stay green through the break.** `grep -c 'auction-player-table\|use-draft-pool' docs/specs/tasks-SE-scoring-editor.md` → **0** before this amendment: no SE task named either file. `auction-player-table-ops.test.ts` only ever passes flat `{ receptions: n }` shapes, so the unit suite cannot see it either, and SE.10(4)'s browser pass as originally written reads sample-line totals inside the editor — never the draft room's Proj column.

**This is not a live bug.** Format 2 does not exist yet, so nothing is broken on `main` today. It becomes a shipped regression **the moment SE.5/SE.9 attaches the first fork row** to a league that then drafts — and it lands exactly on CLAUDE.md's stated 2026 feedback focus, *auction + custom scoring*. Scope is auction rooms only (`draft-room.tsx` gates the table on `isAuction`); standalone mocks pick from templates through the same reader, so they are unaffected until a mock can inherit a fork (deferred — CLAUDE.md's mocks-lead ruling).

**Amendment landed (cheapest shape):** **SE.2 gains a sixth deliverable** — `scoringFamilyFromRules` reads *through* the envelope, which the resolver's own identity property makes a **no-op for format 1**. **SE.10(4)'s browser pass must read the draft room's Proj column on a customized league**, not only the editor's sample lines. Filed as ledger **F133** so the obligation has an id that survives the next drift of this file (R51 — a hand-off without its own F-row is itself a review finding).

### B. SE.6 planned a hook that already ships

`use-league-scoring.ts` (SE.6 item 2) would be a near-duplicate. **`useLeagueScoringFamily` landed 2026-08-20 — 16 hours after this breakdown MERGED, ~35 hours after it was written** (`src/hooks/use-draft-pool.ts`, commit `4e7170a` / L.C3.3 #177) and was extended by MP.6c (`66c415b` / #199) to resolve a standalone mock's template id. SE.6 is amended to make the disposition explicit (CLAUDE.md's no-near-duplicate rule points at wrap-or-extend) and to name the query key nothing in `src/` currently invalidates — see §0(C) and the SE.6 banner.

### C. The invalidation hole SE.6 must close

`auctionPoolKeys.scoring = (leagueId) => ['league-scoring-family', leagueId]` with `staleTime: 10 * 60 * 1000`. `grep -rn "auctionPoolKeys.scoring\|league-scoring-family" src/` → **exactly 2 hits: the definition and the one use.** Nothing invalidates it. A fork or a save that does not invalidate this key leaves an open draft room serving **pre-edit scoring for up to ten minutes**, with no error and no empty state — CLAUDE.md's *"never let 'nothing happened' mean 'it worked'"* shape exactly.

### D. Index of the staleness corrections (all landed in this PR)

| Where | What was stale | Measured replacement |
|---|---|---|
| §1 (`draft_start_internal`), D168 | body cited at `084:486` / PERFORM at `084:681` | **`098_manual_nomination_order.sql:164` / `:362`** — replaced twice since (092 AP.1, 098 AP.5). **D168's argument is STRENGTHENED** — see its own text. |
| §1, D166, SE.4, SE.4b, SE.5, §7 rule 3 | chain heads 087/036; "expected 090/091/092 + pgTAP 039+" | heads **102 / 050**; all four expected numbers **consumed**. Next-free at task time. |
| §1, D166, §7 rule 3 | citation `tasks-M3 §7:360`, and the 083–089 / 032–038 bands as live constraints | **`§7:361`** (line 360 is blank); both bands are 13 numbers behind the head and constrain nothing. |
| §2, SE.1, SE.4, SE.4b, SE.5, SE.6, SE.7, SE.8, SE.9 | 13 of 15 spec line citations | spec v2.11 → **v2.16.6**, 2424 → 2552 lines; **all target text byte-unchanged** — a pure re-point. Re-cited by section + distinctive phrase per §4 rule 9. |
| SE.5(3) | "copy **verbatim** from `spec:1429–1434`" | **the highest-risk citation in the file.** `sed -n '1429,1434p'` today returns `team_managers` indexes and `CREATE POLICY "Stints viewable by league members"` — **a different table's RLS policy**, plausible and syntactically valid. Now cited by policy name. |
| §1, SE.9(1) | picker consumed by "**BOTH**" the wizard and settings | **THREE** mounts — `mock-launch-dialog.tsx:286` (MP.4) is the third, and it has **no league**. |
| SE.6(3) | "a one-line **v2.11.x** changelog entry" | **unexecutable** — `grep -c '^- \*\*v2\.11\.'` → 0, and 23 entries sit above v2.11; minting one would *lower* the header from 2.16.6. Now: next free PATCH above the head at build time. |
| §3 header | "D166–D175 … enter PROGRESS §4 verbatim when this PR merges" | **the fold never ran** — `grep -cE '^- \*\*D1(6[6-9]\|7[0-5]) ' PROGRESS-leagues.md` → **0**. Transcribed in this PR. |
| SE.9(1) | "the existing picker test's card-count/**selection pins**" | `scoring-template-picker.test.ts` has **zero** render assertions (`grep -c 'render(\|@testing-library'` → 0) — it is pure-ops and cannot catch a Customize leak on any of the three mounts. SE.9 now owes a real render pin. |
| ACTIVE-BUILD.md / PROGRESS §2 | M3 "in build"; SE gated on PR #161; "AP and SE contend for 091+/039+" | M3 **complete** (gate passed 2026-08-25); #161 **merged 2026-08-19**; AP has **zero** unchecked tasks and 091/039 are consumed. Corrected in those files by this PR. |

### E. What was re-measured and is UNCHANGED — do not re-derive

SE.1's premises all still hold, so **SE.1 is takeable today** with the one citation fix below: the registry census `grep -c "{ key: '" src/lib/leagues/stats/stat-keys.ts` → **61** (= 48 scorable + 9 context + 4 reserved); `StatKeyDef` and `STAT_KEYS` still land; `derive-stats.ts`'s `DEF_PA_BUCKETS` / `DEF_YA_BUCKETS` / `deriveTierIndicators` and `templates.ts`'s `SCORING_TEMPLATES` are byte-untouched; all four suites SE.1(5) must keep green exist unmodified. SE.1 has **no migration and no DB surface**, so none of §0(D)'s number corrections can collide with it.

---

## 1. What shipped that this builds on (surveyed 2026-08-18; every fact file:line-verified against `main` @ `7061460`)

> **[amended 2026-08-25]** This survey's base is `7061460`. Where a fact below has moved it is corrected in place and marked; where it is still true it has been re-measured against `6b1e4e5` and is listed in §0(E). **§0(A) is the one thing this survey MISSED rather than outlived** — it asked what consumes `scorePlayerWeek` and never asked what else reads the rules document.

- **The calculator is generic and stays untouched.** `scorePlayerWeek(rules, stats)` (`src/lib/leagues/scoring/calculator.ts:66`) is the key-agnostic §7.3.3 dot-product; corrupt (non-finite) coefficients throw loudly, undelivered stats go to `pending`, rounding is `roundHalfUp` (`calculator.ts:45`, half-away-from-zero per the v2.8.4 erratum). The resolver sits **in front of** this function; no line of it changes in this track.
- **Tier derivation owns today's boundary literals.** `deriveTierIndicators` (`src/lib/leagues/scoring/derive-stats.ts:137`) one-hots `def_pa_*`/`def_ya_*` from the raw sources at scoring time; the boundaries are explicit literals in `DEF_PA_BUCKETS` (`derive-stats.ts:65` — shared family + ESPN divergent keys in one table) and `DEF_YA_BUCKETS` (`derive-stats.ts:86`). Semantics to preserve exactly: F20 delivered-zero families, R58/D58 unmappable-source withholding (fractional/negative-PA → family withheld — note `def_pa_0` is `lo: 0` at `derive-stats.ts:67` while `def_ya_0_99` is open-below at `:87`), and the all-zero-family-unconstructible invariant.
- **The three template families this must regenerate byte-exactly.** `SCORING_TEMPLATES` (`src/lib/leagues/scoring/templates.ts:216`) authors the 6 parity templates; ESPN rows carry the ESPN PA family + the YA table (`templates.ts:119`, `:130`), Yahoo/Sleeper the shared PA family only (`:147`). `templates-db.test.ts` deep-equals the seeded rows against these exports; `template-parity.test.ts` carries the 30 hand-computed cent pins. **Both suites must end this track green and unmodified** — that is the "template QA matrix untouched" clause of §7.3.3.1's QA bullet.
- **The registry has no `scoring_surface` yet.** `STAT_KEYS` (`src/lib/leagues/stats/stat-keys.ts:58`, 61 entries; `StatKeyDef` at `:46`) carries `key/label/tier/storage(/column/placeholder)` only. Census against §7.3.3.1's pinned section catalog: **48 scorable** (15 offense incl. `qb_sack_taken` + 6 kicking + 7 D/ST events + 11 `def_pa_*` + 9 `def_ya_*`), **9 context** (`pass_attempts`, `pass_completions`, `rush_attempts`, `targets`, `fg_made`, `fg_attempted`, `pat_attempted`, `def_points_allowed`, `def_yards_allowed`), **4 reserved** (`pass_300_bonus`, `rush_100_bonus`, the two D15 placeholders). 48 + 9 + 4 = 61 = the whole registry (counted by `grep -c "{ key: '"`).
- **`scoring_systems` needs no new columns (§12.25).** Table from 001 (`001_initial_schema.sql:181` — `id/name/description/owner_id/is_system_default/rules/created_at/updated_at`; **no `deleted_at`**), `is_template` + the ownerless CHECK from 058 (`scoring_systems_template_ownerless`, `058_scoring_templates.sql:115`). RLS today: system-default SELECT + own-rows SELECT + **"Users can manage own scoring systems" FOR ALL** (`001_initial_schema.sql:623`) + 058's world-readable template SELECT. That owner FOR ALL is the documented bypass §12.25 names — a commissioner can hand-write an arbitrary doc into their own row via PostgREST, which is **why the snapshot-time backstop exists** (§12.25's "integrity backstop" sentence).
- **Snapshot writers — the three places `scoring_rules_snapshot` is set.** (1) `snapshot_league_scoring_internal` (`059_scoring_snapshot.sql:178`; UPDATE at `:235`), called by **`draft_start_internal`**'s auction-era body (~~the PERFORM at `084_auction_start_budgets.sql:681`, inside the CREATE OR REPLACE at `084:486`~~ **[corrected 2026-08-25 — the head moved TWICE since this survey.** `grep -n "FUNCTION draft_start_internal" supabase/migrations/*.sql` → `066:536`, `084:486`, **`092:395`** (AP.1), **`098:164`** (AP.5). The current head is the CREATE OR REPLACE at **`098_manual_nomination_order.sql:164`**, and its `PERFORM public.snapshot_league_scoring_internal(p_league_id);` is at **`098:362`**. **Sweep the head yourself at task time** — the same `grep`, the sentence §1 already carries for `update_league_settings`; this line has now been wrong once.**]**; `draft_start` itself is the `066_draft_core_rpcs.sql:518` wrapper delegating at `066:530`) — snapshot-BEFORE-transition, D43. (2) `update_league_settings`' pre-draft re-freeze (`061_update_league_settings.sql:218` — copies the new row's rules inline when a snapshot already exists). (3) Nothing else — the D43 guard trigger `trg_leagues_snapshot_guard` (`059:147`) enforces non-NULL in `drafting`+ but validates nothing about the doc's content. **A column-level guard covers all three writers and every future one mechanically** — see D168.
- **The attach path refuses custom rows today.** `update_league_settings` step 5 (`061:205–213`) requires `is_template = TRUE AND owner_id IS NULL` whenever `p_scoring_system_id` is passed; `create_league` has the same check (`060_create_league.sql:198`). The client only sends `scoring_system_id` when it changed (`settings-panel.tsx:369–371`), so keep-current is safe — but §7.3.8 v2.11 ("a template OR the league's own §7.3.3.1 forked custom row — nothing else") requires the 061 check amended (D169). `create_league` stays untouched — a league is always born on a template (D170). **Sweep (2026-08-18): `grep -rln "FUNCTION update_league_settings" supabase/migrations/` hits 061 ONLY across the whole 001–087 chain** — no later migration replaced it, so SE.5's CREATE OR REPLACE authors against 061's body at HEAD. **[re-measured 2026-08-25 — STILL TRUE across 001–102**: the same `grep` still hits `061_update_league_settings.sql` and nothing else, so SE.5 authors against 061's body. Contrast `draft_start_internal` above, which moved twice in the same window — **run the grep, do not inherit this line.**]
- **The picker is shared and controlled.** `scoring-template-picker.tsx` (selection-only; ~~consumed by BOTH the create wizard and the settings panel~~ **[corrected 2026-08-25 — there are THREE mounts now, not two.** `grep -rn "ScoringTemplatePicker" src/ | grep -v scoring-template-picker` → `league-create-modal.tsx:268` (the wizard), `settings-panel.tsx:481` (settings), and **`src/components/draft/mock-launch-dialog.tsx:286`** — the MP.4 standalone-mock launcher, added 2026-08-24 (`6e9e8d1`). **The mock mount has NO LEAGUE**: it passes only `value`/`onChange`, and `drafts.league_id` is nullable (migration 095). The picker's own docblock also still says "BOTH" — SE.9 corrects both. Any league-context prop D170 adds MUST default to "no league" so this mount keeps rendering templates-only.**]** — its own docblock forbids forking it) + `scoring-template-picker-ops.ts` + `useScoringTemplates` (`src/hooks/use-scoring-templates.ts` — world-readable template rows, display order client-side). §16.2 v2.11 says this component **gains "Customize"** (~~spec:1624~~ **[corrected — §16.2's `scoring-template-picker.tsx` row, "template cards + compare"; now at `spec:1717`, cite it by the row's file path]**); the two new components (`scoring-editor.tsx`, `scoring-editor-ops.ts`) do not exist yet in `src/components/leagues/` (re-verified by listing 2026-08-25 — still absent).
- ~~**Chain heads at survey time:** migrations = **087** (`087_auction_commish_controls.sql`); pgTAP = **036**. tasks-M3 §7 reserves **088** (L.C1.6) and **089** (L.C1.7) whether or not they have landed — the same sentence reserves **pgTAP 032–038** (tasks-M3 §7:360), so SE numbers start **above BOTH bands** — migrations above 089 (expected 090/091/092 — three SE migrations after the D175 wall split), pgTAP above 038 (expected 039+) — whether or not L.C1.6/L.C1.7 have landed. **PR #160 (merged 2026-08-18, after this survey's base) additionally queued tasks-M3 L.C1.8 with a FLOATING "migration 088+ (next free number at build time)" claim** — a claimant that can consume 090/091 ahead of SE.4/SE.5, which is why those numbers are EXPECTED, never promised.~~
- **[corrected 2026-08-25] Chain heads — EVERY NUMBER ABOVE IS CONSUMED; the reservation bands no longer constrain anything.** `ls supabase/migrations/ | tail -1` → **`102_mock_slot_choice.sql`**; `ls supabase/tests/ | tail -1` → **`050_mock_slot_choice.sql`**. The four numbers the struck text "expected" were all taken, three of them by lanes that did not exist when it was written: **090** = `090_snake_pause_first.sql` (L.C1.8 — landed 2026-08-18, *one day before this doc merged*, exactly the floating claimant the struck text warned about), **091** = `091_mock_cpu_reactive_bidding.sql` (AP.3), **092** = `092_auction_reserve_toggle.sql` (AP.1), **pgTAP 039** = `039_mock_cpu_reactive_bidding.sql` (AP.3). tasks-M3 §7's **083–089 / 032–038** bands are now 13 numbers behind both heads and are **dead letters** — do not reason about them. *(Its sentence is at **`tasks-M3 §7:361`**, not `:360` — line 360 is blank.)* **The operative rule is unchanged and it is the only thing that saved this track: take the next free number at task time under D161's grep-R/D/F-and-numbers-together-across-open-branches rule, confirmed with `ls` in the migration banner. Heads measured 2026-08-25 are 102 / 050 — so expect 103/104/105 and pgTAP 051+, and CONFIRM, because that expectation has now been wrong once.**
- **No scoring fan-out exists yet** (M4/M5): nothing in `src/` calls `scorePlayerWeek` against a league snapshot in production paths — the resolver's engine consumer today is the editor's live sample lines + the test suites; M4 inherits `resolveRules` from the engine home (D167) with zero rework. **[amended 2026-08-25 — STILL TRUE, AND IT WAS THE WRONG QUESTION.** Re-measured: `grep -rn "scorePlayerWeek" src/ | grep -v '\.test\.'` → only `calculator.ts:66`, its own definition. But `scorePlayerWeek` is not the only thing that **reads the rules document**: `scoringFamilyFromRules` (`src/components/draft/auction-player-table-ops.ts`) reads a **top-level `receptions`** key off it to pick the auction room's projection column, and a format-2 envelope has no top-level keys. **Read §0(A) before writing SE.2** — this bullet's true sentence is why no SE task owned that path.**]**

---

## 2. Read together (every SE session, in this order)

1. `CLAUDE.md` — Active Builds + the migration-discipline and never-let-nothing-happened rules.
2. `docs/specs/spec-redraft-leagues.md` **v2.11+** — **§7.3.3.1 IN FULL** (the LAW this track implements), §7.3.3 (snapshot/precision/extensibility law that stays), §7.3.8 (the v2.11 validation bullet), §12.25, §16.2, §16.5.5, §23.5 (the v2.11 registry bullet), Appendix B.4 + its v2.11 note, the v2.11 changelog entry.
   > **[corrected 2026-08-25 — 13 of the 15 spec line numbers this doc cited had drifted; every one of them is re-pointed here, and THE TARGET TEXT IS BYTE-UNCHANGED in all 13 cases.** The spec went **v2.11 → v2.16.6** and 2424 → 2552 lines while this breakdown sat unrevised. **Per §4 rule 9 the durable citation is the section number plus a distinctive phrase — cite that, and treat any line number below as a convenience that expires.** Line numbers measured 2026-08-25:
   >
   > | Anchor | Was | **Is (2026-08-25)** | Distinctive phrase — use THIS |
   > |---|---|---|---|
   > | §7.3.8 validation bullet | `436` | **`439`** | "Exactly one scoring system referenced and readable by the league…" |
   > | §12.25 header | `1423` | **`1487`** | `### 12.25 scoring_systems (extend existing) — league custom scoring (NEW v2.11)` |
   > | §12.25 member SELECT policy | `1429–1434` | **`1493–1498`** | `CREATE POLICY "League members read league scoring" ON scoring_systems FOR SELECT` — **see the SE.5(3) warning** |
   > | §15.1 header | `1494` | **`1581`** | `### 15.1 League & membership` |
   > | §16.2 component rows | `1624/1625/1626` | **`1717/1718/1719`** | the rows keyed by `scoring-template-picker.tsx` / `scoring-editor.tsx` / `scoring-editor-ops.ts` |
   > | §16.5.5 | `1750–1751` | **`1844–1845`** | `#### 16.5.5 Deliberately *not* in v1 UI` |
   > | §23.5 registry bullet | `2048` | **`2152`** | `- **Registry additions for the custom editor (v2.11, §7.3.3.1):**` |
   > | App B.4 + its v2.11 note | `2215–2218` | **`2319` / `2322`** | `### B.4 Kicking, D/ST & reserved catalog` |
   >
   > **Only two of the original citations still land: `spec:197` (§7.3's `setup`/`scheduled` window) and `spec:347` (§7.3.3.1(5)'s enforcement sites).** Both re-measured 2026-08-25.]
3. `docs/specs/PROGRESS-leagues.md` — §3 Resolved **Q12/Q13 (the 2026-08-16 scoring-editor pair — rulings + the defense-tier corrections)**, §4 (D33 one-namespace · D44 `dst_model`/derived indicators · D57 calculator mechanics · D59 ownerless templates + score-inert zero · D62 hand-computed literals · D146(1) one-unit boundary pins), §6 (F21, F59, F71, F24), this doc's §3 decisions once merged.
4. `docs/specs/delivery-plan-redraft-leagues.md` §2.3 (DoD — every task cites it) + §8.1–8.4 checklists for schema/RLS/RPC work.
5. The task's own banner in §5 below, plus the §1 survey facts it cites.

---

## 3. Design decisions (Architect; **D166–D175** — they enter PROGRESS §4 verbatim when this PR merges, the D126–D143/D147–D156 precedent)

> **[corrected 2026-08-25 — THE FOLD NEVER RAN.** PR #161 merged 2026-08-19 and D166–D175 were never transcribed: `grep -cE '^- \*\*D1(6[6-9]|7[0-5]) ' docs/specs/PROGRESS-leagues.md` → **0**, and §4 ran straight from D165 to D176. `grep -rn 'D167' docs/` found it in **this file only**. The ledger was internally inconsistent as a result — PROGRESS's own D176 note cites the first-filed-keeps resolution *in favour of ten numbers that were not there*, and **Chris's D175 ruling lived nowhere in the decisions log at all**. Every AP/MS/MP/DR/M3 sibling block exists; this one was the gap. **The amendment PR transcribes D166–D175 verbatim into PROGRESS §4** under a `2026-08-19 — Architect (the SE scoring-editor breakdown; transcribed verbatim from tasks-SE-scoring-editor.md §3 at merge)` header. **No renumbering** — D176's own note already records first-filed-keeps in SE's favour, so the numbers reserved here are the numbers that land.]

- **D166 — Track charter and lane law.** SE is the C37 separate parallel track: prefix `SE.`, its own §5 dependency graph, **never blocks and is never blocked by `DR.*` or `L.C*`**. The only shared surface is `PROGRESS-leagues.md` (append-collisions resolved at merge — the established two-lane pattern). Loop precedence (recorded in ACTIVE-BUILD by this PR): DR.* first, then L.C*, then — when both are blocked, or when Chris directs — the next unblocked SE.* task. Migration/pgTAP numbers: ~~SE never takes a number inside tasks-M3 §7's reservation bands — **migrations 083–089 AND pgTAP 032–038** (both reserved in the same §7:360 sentence)… expected migrations 090/091/092 (SE.4/SE.4b/SE.5) and pgTAP 039+~~ **[corrected 2026-08-25 — the bands are dead and every expected number is consumed** (§1's corrected chain-heads bullet has the four claimants by filename; the reservation sentence is at **`tasks-M3 §7:361`**, not `:360`). **What survives is the rule, and only the rule: SE takes the NEXT FREE number measured at task time** with `ls supabase/migrations/ | tail -1` and `ls supabase/tests/ | tail -1`, under the D161 grep-R/D/F-and-numbers-together-across-open-branches check, recorded in the migration banner. Heads on 2026-08-25 were **102 / 050**; SE.4/SE.4b/SE.5 should therefore expect **103/104/105** and **pgTAP 051+** — an expectation, never a promise, which is precisely the distinction that kept this track from colliding.]
- **D167 — The engine home rule: format, resolver, and cut-list math live in `src/lib/leagues/scoring/`, not in a component ops file.** `resolveRules`, the format-2 types, `tierKeysFromCuts`, and the named cut-list constants are **scoring law** — M4's fan-out will call the resolver on every frozen snapshot, and one implementation must serve editor, engine, sim, and tests (the D33/D5 one-namespace argument applied to code placement). `scoring-editor-ops.ts` **composes and re-exports** them for the editor plus the editor-only pieces (section catalogs, switch-state derivation, edit application); §16.2's `scoring-editor-ops.ts` row (~~:1626~~ **[corrected 2026-08-25 → `spec:1719`]**) listing names the surface the editor consumes, which this satisfies — it is not an instruction to duplicate engine math into `src/components/`.
- **D168 — Enforcement architecture: one validator per language, and the backstop guards the COLUMN, not a function body.** (1) TS `validateScoringRulesDoc` (client Zod/UX + service pre-checks) and SQL `scoring_rules_validate(jsonb)` (every server write path) implement the same five §7.3.3.1 guardrail families, tied by a **TS≡SQL parity fixture** (the D90/M3 max-bid precedent): one shared table of accept/reject docs run through both, so the mirrors cannot drift silently. (2) The draft-start/§7.3.8 backstop is a **new BEFORE INSERT OR UPDATE trigger on `leagues`** validating any non-NULL `scoring_rules_snapshot` being written — NOT a `CREATE OR REPLACE` of `draft_start` or `snapshot_league_scoring_internal`. Reasons: it covers all three §1 writer sites *and every future writer* mechanically (the D43 pattern, extended from "non-NULL" to "valid"); it is purely additive, so the SE schema lane never edits a function the M3 lane owns (zero cross-lane serialization); and it is immune to the CREATE-OR-REPLACE-reversion failure class (CLAUDE.md's migration-073 lesson — a later replace of `draft_start` authored against an older body cannot silently drop a guard that lives on the column). *(Extended by D175 — Chris's 2026-08-18 ruling moves the FIRST wall up to `scoring_systems` writes themselves; this decision's leagues trigger stays as the second wall, and both land in SE.4b.)* §7.3.3.1(5) delegates the exact split to this breakdown; §12.25's "re-validation in `draft_start`" requirement is satisfied mechanically — `draft_start_internal` PERFORMs the snapshot write (~~084:681~~ **[corrected 2026-08-25 → `098_manual_nomination_order.sql:362`, inside the CREATE OR REPLACE at `098:164`]**; `draft_start` is the 066:518 wrapper delegating at 066:530) and the trigger makes that write refuse, so a league can never enter `drafting` on an invalid doc, loudly.
>
> **[amended 2026-08-25 — THIS DECISION'S CENTRAL ARGUMENT IS NOW STRENGTHENED BY LIVE EVIDENCE, not weakened by the drift.]** D168(2) chose a **column-level trigger** over a `CREATE OR REPLACE` of `draft_start_internal` partly because a trigger "is immune to the CREATE-OR-REPLACE-reversion failure class" (CLAUDE.md's migration-073 lesson). In the seven days after this doc was written, `draft_start_internal` **was replaced twice** — `092_auction_reserve_toggle.sql:395` (AP.1) and `098_manual_nomination_order.sql:164` (AP.5) — by two lanes that had never read this breakdown. `grep -n "snapshot_league_scoring_internal" supabase/migrations/098_manual_nomination_order.sql` → **`362`**: the PERFORM survived both replaces. Had SE's guard been written *into that function body* on 2026-08-19, AP.1 and AP.5 would each have had to carry it forward blind, and CLAUDE.md's own recorded precedent (migration 073 silently reverting 049/050/051's username guards) says what happens then. **A guard on the column would have needed no author to remember it. Build SE.4b as D168 specifies.**
- **D169 — The 061 attach amendment (`update_league_settings`, CREATE OR REPLACE authored at chain HEAD).** Step 5's check becomes: `p_scoring_system_id` must reference **a template (unchanged predicate) OR the league's own currently-referenced custom row** — any *other* non-template row (personal research systems, another league's fork) keeps the refusal, message updated to cite §7.3.8 v2.11. Re-picking a plain template detaches the fork (the repoint IS the detach — §12.25 orphan hygiene: rows are left in place, the member SELECT policy stops matching on its own). **The spec's settings-ATTACH validation arm (spec:347 "editor save + settings attach"; F21's "at write/attach") is OWNED here:** whenever `p_scoring_system_id` is passed, the RPC `PERFORM`s `scoring_rules_validate` on the referenced row's `rules` BEFORE the write. With D175's table wall in place this arm is **defense-in-depth, not the only guard** — an invalid league doc should be unrepresentable upstream — but it stays because it is the user-facing error surface at settings, it is cheap, and step 6's re-freeze never fires on a same-row re-attach (`v_new_scoring IS DISTINCT FROM v_current_scoring` is false), so this is the attach path's own check. D168's leagues trigger remains the snapshot backstop behind both.
- **D170 — Customize lives in league context; the create wizard stays templates-only.** The fork needs a league row to exist (owner = its commissioner, name defaults `<League> Custom`, `leagues.scoring_system_id` repointed in the same transaction), so the "Customize" affordance renders only where the picker has a league in `setup`/`scheduled` (the settings surface); in the create wizard the picker is unchanged and `create_league`'s template-only check (060:198) is untouched. The wizard MAY show a one-line "you can customize scoring after creating" hint (copy per §16.5.4's designed-copy rule); it may NOT grow a pre-create editor. *(Flagged in §8 for Chris's read on this PR — it is a visible product-shape call, and he holds the merge.)*
- **D171 — Sample lines run the REAL pipeline, and for D/ST that includes the derive helper.** The live total is `scorePlayerWeek(resolveRules(doc, P), deriveTierIndicators(line))` — `resolveRules` + the shipped derive + the shipped calculator, never a parallel math path (§7.3.3.1's own words, made concrete: the Seahawks line stores RAW `def_points_allowed: 17` / `def_yards_allowed: 289` and the one-hot happens exactly where it happens in production). Context numbers (Marino's 24-of-36, Peterson's 22 carries) render as flavor and score nothing — they are omitted from the stat objects entirely rather than zeroed, so a doc that (illegally) referenced an aggregate would surface as pending, not silently 0.
- **D172 — "The editor's own quality gate" (§7.3.3's v2.11 parenthetical) = SE.10.** The gate is the §7.3.3.1 QA-bullet suite enumerated and green in one run on a fresh reset, plus the R278-form browser pass of the whole flow. No new feature flag: the editor rides the existing leagues cohort gate; "GA default posture stays templates-only until the gate passes" is a product statement Chris operates on SE.10's recorded result, not a config surface this track builds.
- **D173 — `return_yards` is a catalog flag, and the flag ships OFF.** The Special Teams section catalog carries the key with `gated: true` from day one (SE.7) so Q13's step (3) is literally one boolean flip — the editor renders sections **from the catalog** and a gated field renders nothing (not a disabled row — §16.5.5's don't-build-deferred-UI posture). The registry entry itself does NOT exist until the data task lands (a rules key whose stat is never delivered is the standing-E61 Q9 lesson). Filed as ledger **F71**; the 4-step checklist is §8's note, not a task.
- **D174 — The §7.3.3.1(a) "first open-below" vs `def_pa_0`-floor reconciliation (a resolved spec-text tension — Builder-carriable, no halt).** **Number note (D161 — R, D and F checked together 2026-08-18 across `main` @ 9a20bc4 and both open branches):** main's high-water is **D165**, and its F57 session row deliberately minted no D166 ("two agents are in flight that were each told D166+"); this branch filed **D166–D173 FIRST** (commit `90cb5a8`, 14:38 PT; PR #161 opened 14:39 PT) and PR #162 (DR.2) filed its own D166 second (commit `14557ca`, 15:01 PT; PR opened 15:03 PT) — so under R312/D144(6) first-filed-keeps this doc keeps D166–D173, this note takes **D174**, and #162's D166 renumbers at its merge (flagged to the orchestrator). The generation rule says tiers are `[cut, next_cut)` "with the last open-ended and the first open-below"; the shipped PA table floors `def_pa_0` at exactly 0 (`derive-stats.ts:67`) and R58/D58 rules negative PA **unmappable** (family withheld), while YA's first bucket is genuinely open-below (`:87` — negative total yards is a real game). The same spec bullet requires the cuts-derived reading to **agree with today's literals** ("no behavior change"; the derive-agreement invariant), and negative PA is unreachable in football — so the controlling text is the agreement clause: `tierKeysFromCuts` affects NAMES only, and the derive helper's cuts-generated PA table keeps `lo = first cut` (R58 semantics pinned), YA keeps open-below. SE.1 pins this exact case (PA −2 → family withheld under BOTH readings).
- **D175 — THE WALL IS THE TABLE: an invalid league scoring document is UNREPRESENTABLE in `scoring_systems` (Chris, ruled in-session 2026-08-18 — verbatim: *"creating an invalid scoring system should not be possible"*).** **Number note (D161):** same counter check as D174 — main's high-water is D165 and this branch first-filed D166+; next free here is **D175**; PR #162's in-flight D166 renumbers at its merge. **(1) What the ruling strengthens:** spec §7.3.3.1(5)/spec:347 enforces door-by-door (editor save + settings attach + draft start). Chris's sentence moves the wall INTO the database at the write: a BEFORE INSERT OR UPDATE trigger on `scoring_systems` runs `scoring_rules_validate(NEW.rules)` whenever the row is in the **league profile** (below), so NO write path — including 001:623's legacy owner FOR ALL, the §12.25 bypass that motivated the backstop in the first place — can create an invalid league doc. The spec's write/attach/draft-start sentence is satisfied **a fortiori**; the RPC, attach, and snapshot checks remain as layered defenses (cheap, and they carry the user-facing error at the right surface). This is a **ruling, not an erratum** — it strengthens the spec's MUST. **(2) The boundary (the D33 nuance, designed rather than skipped):** the same table holds the legacy RESEARCH systems, whose flat docs use the legacy column-name namespace (D33 — `src/lib/scoring/default.ts`'s world) that the strict league validator would wrongly reject, so "valid" is context-dependent and the trigger validates exactly the **league profile**: `is_template = TRUE` (canonical format-1 docs, migration-authored — all 6 pass, pinned) **OR** `NEW.rules ? 'format'` (a format-2 envelope is definitionally a §7.3.3.1 league doc — `format` is not a registry key and no legacy doc carries it) **OR** the row is **live-league-referenced** (`EXISTS (SELECT 1 FROM leagues WHERE scoring_system_id = NEW.id AND deleted_at IS NULL)` — the §12.25 member-policy's own predicate; this arm closes the one hole a format-only profile would leave, a fork row REWRITTEN as an invalid flat map while attached). Research rows match none of the three → writable under their own rules, untouched, no legacy-row migration (constraints (a)/(b)/(c) of the boundary charter all hold). **(3) Coverage of every league surface:** fork INSERT is format 2 (format arm, before the same-transaction repoint lands); editor saves are format 2 + referenced; corrupt-while-attached is caught by the reference arm; a DETACHED fork row corrupted to a flat map is just a personal row again — re-attach validates (D169's arm) and any snapshot validates (D168's leagues wall), so an invalid doc still can never score. **(4) Existing rows — never retroactively invalidated:** the trigger fires on writes only. At build time the profile matches exactly the template rows (all league references today are templates); SE.4b counts them and shows each passes. Legacy research rows sit OUTSIDE the profile and are validated only if they ever enter it — and every door INTO the profile (fork, attach) validates first. **(5) Task shape:** the two walls (this trigger + D168's leagues trigger) land together in **SE.4b**, split out of SE.4 so neither task exceeds the half-day ceiling. **(6) [added 2026-08-25 — A SECOND REFERENCE PATH now exists that the profile deliberately does NOT match.]** MP.4 gave standalone mocks their own scoring reference at `drafts.config->>'scoring_system_id'` (`src/components/draft/mock-launch-ops.ts:179`), which the league-profile's `EXISTS (… FROM leagues …)` arm **cannot see** — so a scoring row referenced *only* by a mock draft sits outside the profile and stays writable under its own rules. **That is the correct outcome and it is stated here so the silence does not read as an oversight:** standalone mocks pick from the six shipped templates (which match the profile via `is_template = TRUE` anyway), the reference is deliberately unvalidated server-side (`src/lib/leagues/api/draft-service.ts:1615` records the rule-10 reasoning; the *shape* is checked — `z.uuid()` at `:1633`), and a mock scores nothing that persists. **SE.4b(4) owes one pgTAP line for it:** a non-template, non-league-referenced row that a *mock* references stays writable with a legacy-shaped flat doc — the D33 protection, pinned at the second reference path as well as the first.

---

## 4. Standing rules for every SE task

Rules 1–4 of tasks-M1/M2/M3 §4 carry forward **verbatim and in full force** for all schema/RPC work: grants doctrine D18→D23 · SECURITY DEFINER = in-body auth + `search_path=''` + REVOKE (§4.1) · no-write-policy pgTAP pattern per role with RETURNING counts (§4.2) · falsifiability floor — golden pins as stored literals, boundary instants, ≥1 deliberate-break probe SHOWN failing then reverted (§4.3) · migration checklist + R6/D38 waivers cited in the banner (§4.4). Plus:

5. **The byte-identity floor (this track's never-weaken addition).** Templates stay format 1 and byte-identical: no SE diff touches `templates.ts` rules objects, migration 058's seeds, `template-parity.test.ts`, or `templates-db.test.ts` except to ADD suites beside them. The three regeneration pins (cut lists → today's key names, SE.1) and the resolver identity property (SE.2) join the never-weaken class — no later task may delete or loosen them. A diff that changes any shipped `derive-stats.ts` literal has left scope.
6. **One-unit boundary pins (D146(1)).** Every ≥/≤/count guardrail carries a negative fixture exactly one unit from the boundary: `|coef| = 100.00` accepts / `100.01` rejects; `0.01` accepts / `0.001` rejects (2dp); the bucket-exclusivity pair is the F21 literal itself (`def_pa_14_20` + `def_pa_18_27` in one doc → named rejection); position-scope's one-unit case is a single K key under `QB`.
7. **UI rigor split (the ruled house posture).** `scoring-editor.tsx` and other component surfaces get the **one-review-pass reskin treatment** (design-system rules: single theme, no `dark:`, tokens only, elevation per the CLAUDE.md ruling, ×0.8 scale); `scoring-editor-ops.ts` and everything under `src/lib/leagues/scoring/` are **ops/engine — they get pins** (leagues-grade). Server-side validation and migrations are always leagues-grade regardless of which task carries them.
8. **Time/stats/typegen discipline.** No raw `Date.now()`/`fetch` in anything league-shaped (TimeProvider/StatsProvider law — the editor needs neither: sample lines are stored literals). Typegen: regenerating `src/types/database.ts` clobbers the hand-written alias block — preserve and re-append it, verify the diff additive-only (standing rule §4.4).
9. **A claim about the state of the world after your change must name the command or observation that establishes it — the falsifiability floor of tasks-M1/M2/M3 §4 rule 3, applied to prose — *no pin counts as a pin until it has been shown RED against the defect it claims to catch*. (In THIS doc the floor is §4's carried rule 3 — the falsifiability floor itself; its home is the milestone docs, and SE tasks inherit it.)** This rule exists because of one measured pattern in DR.1's review (R339–R344, 2026-08-18): **every claim in that PR that had been MEASURED held up** — the `next build` route-manifest diff (136 URL values + 39 static/71 dynamic entries incl. every `namedRegex`, EMPTY), the same-cookie middleware differential in both directions, the F66 404 comparison against a `main` worktree, the shell-provider enumeration — several of them in more detail than they were written up with. **Every claim that failed was the same species: an assertion about the post-change world inferred from the INTENT of the change rather than observed.** "The rename replaced two identifiers so the file is still 1013 lines" (true of the rename, false of the commit — it was +22). "The other arms have buttons, so no state is a dead end" (four arms checked, two never enumerated — both had no exit at all). "The false docblock claim WAS corrected" (one of its two false sentences was). "`route-groups.test.ts` pins that" (pin written, path recalled — it is `src/lib/`, not `src/app/app/`). "Replayed with the identical cookie" (named in the helper, never delivered to `request.cookies`). Each was written in the *proved* register beside claims that genuinely were proved, and inherited their credibility; **each was one `grep`, one `wc -l`, or one DOM inventory away from being caught.** So, concretely: a sentence of the form *"X still lands"*, *"Y is not a dead end"*, *"Z was corrected"*, *"the pin covers it"* is **not shippable without the command or measurement beside it** — in the PR body, the docblock, or the PROGRESS entry. Prefer citing by **symbol** over line number where you can; a symbol survives the next edit. If you cannot cheaply establish it, write what you actually know ("not checked") — a hedge is free and a false disclosure costs the next Builder a day. *(Adopted from tasks-DR §4 rule 9 — byte-identical except the opening parenthetical, re-pointed from tasks-DR's own §4 structure to this doc's; SE tasks inherit it in full.)*

---

## 5. Task list (one Builder session each; ≤ half a day)

Dependency order:

```
ENGINE   SE.1 (registry + cuts + generation) → SE.2 (format 2 + resolver + fork doc) → SE.3 (the guardrail validator, TS)
SQL      SE.3 → SE.4 (validate fns + parity) → SE.4b (the two write walls — D175) → SE.5 (fork/save RPCs + §12.25 policy + 061 amendment)
API      SE.5 → SE.6 (routes + hooks + §15.1 erratum)
UI       SE.6 → SE.7 (ops + editor shell) → SE.8 (K/D-ST pages + the six live sample lines)
         {SE.6, SE.7} → SE.9 (Customize entry + fork flow + member view)
VERIFY   everything → SE.10 (the editor's quality gate)
```

SE.1–SE.3 are pure TS (no migration, no stack writes) and may run while other lanes hold the schema lane. SE.4/SE.4b/SE.5 are the track's schema lane, serialized, numbers per D166. SE.7's ops-layer half depends only on SE.3, but the task ships component + ops together — do not split it.

### SE.1 — Registry `scoring_surface` + the named cut lists + tier-key generation (the byte-exact pins)
> Read spec §23.5's v2.11 registry bullet (~~spec:2048~~ **[corrected 2026-08-25 → `spec:2152`; cite it by its opening phrase, `- **Registry additions for the custom editor (v2.11, §7.3.3.1):**`. Line 2048 today is §21's Open Question 16 (charted-data licensing) — a live, plausible, entirely wrong target. The bullet's text is byte-unchanged.]**) + §7.3.3.1's editable-scope and D/ST-tiers bullets, §1 here (the 48/9/4 census + the boundary-literal facts), D166/D167/D174, `stat-keys.ts`, `derive-stats.ts`, `templates.ts`. No dependencies — opens the track.
>
> **[status 2026-08-25 — SE.1 IS CLEAN AND TAKEABLE TODAY.** Every premise re-measured and unchanged (§0(E)): census `grep -c "{ key: '" src/lib/leagues/stats/stat-keys.ts` → **61**; `StatKeyDef`, `STAT_KEYS`, `DEF_PA_BUCKETS`, `DEF_YA_BUCKETS`, `deriveTierIndicators`, `SCORING_TEMPLATES` all still land; the four suites in (5) exist and are byte-untouched. **No migration and no DB surface**, so none of §0(D)'s number corrections can collide with it. The citation above is the only fix this task needed.]
>
> 1. `StatKeyDef` gains **`scoring_surface: 'scorable' | 'context' | 'reserved'`** (required, no default — every entry classified explicitly). Classification per the §1 census: 48/9/4. Pin: the scorable set ≡ (the §7.3.3.1 section-catalog keys ∪ the cut-generated tier-key names — both PA families + the YA family, 20 keys) — an exact set-equality test, not a count.
> 2. Named cut-list constants in `src/lib/leagues/scoring/` (D167): `SHARED_PA_CUTS = [0,1,7,14,21,28,35]`, `ESPN_PA_CUTS = [0,1,7,14,18,28,35,46]`, `YA_CUTS = [0,100,200,300,350,400,450,500,550]` — §23.5(b)'s definitions as code.
> 3. **`tierKeysFromCuts(prefix, cuts)`** — tiers `[cut, next_cut)`, last open-ended, names `<prefix>_<lo>_<hi>` with `hi = next_cut − 1`, single-value first tier → `<prefix>_0`, final tier → `<prefix>_<lo>_plus`. **THE REQUIRED PIN (spec's own words: "pin this equivalence by test — it is the whole backward-compatibility argument"):** the three calls regenerate today's families **byte-exactly** — shared PA → the 7 shared keys, ESPN PA → the 8 ESPN keys, YA → the 9 YA keys — asserted as stored literal arrays AND cross-checked against `DEF_PA_BUCKETS`/`DEF_YA_BUCKETS` key sets and the keys actually referenced by `SCORING_TEMPLATES` rules objects.
> 4. `deriveTierIndicators` gains an **optional cuts parameter defaulting to today's literals — no behavior change** (§7.3.3.1(a) verbatim): cuts generate a `TierBucket[]` with PA flooring at the first cut and YA's first bucket open-below (D174 — pin the PA = −2 case withheld under BOTH readings, and the fractional case likewise). **Derive-agreement invariant pin:** for every integer PA 0–60 and YA −20–620, literal-table derivation ≡ cuts-derivation for both families.
> 5. No migration, no DB surface. Existing suites (`derive-stats.test.ts`, `templates.test.ts`, `template-parity.test.ts`, `templates-db.test.ts`) stay green **unmodified** (§4 rule 5).
>
> DoD: plan §2.3; §4 rules 5/6/9; `npm run test` + `type-check` + `lint` clean (stack up per D59(5)); break probe — flip one ESPN cut (18 → 19) and show the byte-exact pin AND the agreement invariant go RED at named counts, then revert.

### SE.2 — Format 2: the envelope types, `resolveRules`, normal form, and the fork doc
> Read **§0(A) FIRST** (the added deliverable (6) and why it exists), then spec §7.3.3.1's rules-document + All-Positions + D/ST(b) bullets, D59(4) (explicit-0 score-inert), D167, SE.1's exports. Depends SE.1.
>
> 1. Types in `src/lib/leagues/scoring/` (D167): `ScoringRulesDoc` = format 1 (flat map — **no `format` member; `format` is not a registry key, pin the discriminator**) | format 2 (`{ format: 2, base, positions: {QB,RB,WR,TE,K,DST partial}, tier_cuts: { def_pa, def_ya } }` — the spec's printed shape).
> 2. **`resolveRules(doc, position)`** — pure: format 1 → the doc itself for EVERY position (**the identity property, property-pinned** across the six positions + an arbitrary string — this is the zero-scored-outcome-change guarantee); format 2 → `{...base, ...positions[P]}` (override wins; explicit `0` switches the category off for that position — pinned; absent position, including any position outside the six → `base` alone — pinned).
> 3. **`normalizeScoringDoc(doc)`** — strips overrides equal to the base value and empty override objects (§7.3.3.1's normal-form bullet); round-trip pin in the `src/lib/leagues/settings/` discipline: normalize ∘ normalize ≡ normalize, and the All-Positions switch state (SE.7's derivation) round-trips from the doc alone.
> 4. **`forkTemplateDoc(templateRules)`** — builds the format-2 fork: `base` = the template's flat map verbatim, `positions` empty, `tier_cuts` = the inherited family's cut lists (ESPN templates ⇒ `ESPN_PA_CUTS` + `YA_CUTS`; Yahoo/Sleeper ⇒ `SHARED_PA_CUTS` + `YA_CUTS` — the YA slot is always written per the printed shape; a single-model doc simply pays no `def_ya_*` keys, and ADDING that table is F59's follow-up, not this editor). Family detection is by the template's own `def_pa_*` key set against the two generated sets — never by name-matching "ESPN".
> 5. **Fork-equivalence property (QA bullet):** for all 6 templates × all 6 positions × the L.A1.10 parity fixture lines, `scorePlayerWeek(resolveRules(forkTemplateDoc(T), P), stats)` ≡ `scorePlayerWeek(T, stats)` — totals AND perKey AND pending.
> 6. **[ADDED 2026-08-25 — THE ENVELOPE MUST NOT BLIND THE AUCTION ROOM. Read §0(A) in full before writing this; it carries the measured consequence chain. Ledger F133.]** `scoringFamilyFromRules` — in **`src/components/draft/auction-player-table-ops.ts`**, the function whose body reads `const perReception = (rules as Record<string, unknown>).receptions` (**cite it by symbol; the line was `:102` on 2026-08-25 and this file has drifted before**) — **must read through the format-2 envelope.** It is the auction room's only scoring reader and it is **not** a `scorePlayerWeek` consumer, which is why §1's survey did not find it and why no SE task owned it before this amendment.
>     - **Shape:** resolve first, then classify — e.g. `scoringFamilyFromRules(resolveRules(doc, 'WR'))`. `receptions` is a WR-relevant coefficient and a per-position override of it is exactly the case the room must reflect. **By SE.2(2)'s identity property this is a strict no-op for format 1** — that is the whole reason this is the cheapest correct shape, and the reason it cannot regress any template league.
>     - **Both hook arms are affected and neither needs its own fix if this one lands:** `useLeagueScoringFamily` (`src/hooks/use-draft-pool.ts`) returns `leagues.scoring_rules_snapshot` — which SE.4b(5) pins as the format-2 doc **verbatim** — or falls back to `scoring_systems.rules` directly. Both hand the raw document to this function.
>     - **THE PIN:** a forked ESPN Full PPR doc (`forkTemplateDoc` of the template) resolves to **the same family as its template** — assert on the fork, not only on flat maps. `auction-player-table-ops.test.ts` today passes only flat `{ receptions: n }` shapes, which is precisely why its suite would have stayed green through the break; **add the format-2 cases beside them, do not modify the flat ones** (§4 rule 5's add-beside discipline).
>     - **Scope discipline:** this is a read-path change inside one exported function plus its test. It is **not** licence to touch `decorateRows`, the table component, or the hook. If the fix appears to need more than that, stop and file it.
>
> DoD: plan §2.3; §4 rules 5/6/9; suites green; break probe — ~~make `resolveRules` merge `positions[P]` under format 1 too (simulating the identity break) and show the identity property + fork-equivalence RED, revert~~ **[corrected 2026-08-25 by SE.2's Builder — THIS PROBE CANNOT FAIL, and it was measured rather than argued.** Applied verbatim (the format-1 arm merging `positions?.[P]`, returning the document unchanged when there is none), the suite ran **296/296 GREEN**: a format-1 document HAS no `positions` member, so the prescribed mutation is a literal no-op on every input that exists. This is D267(8)'s own rule pointed back at the plan that inherited it — *a pin whose fixture makes the guarded line unreachable is decoration*, and so is a probe whose mutation is unreachable. **The probe that DOES guard the identity property: make the format-1 arm read the document as an envelope** (`return { ...doc.base }` ⇒ `{}`) ⇒ **15 RED / 281**, of which **10 are pre-existing cases** in `auction-player-table-ops.test.ts` — the 9 parametrized `receptions n → family` cases plus *"the three shipped template values map to the three families"* — i.e. the identity break blanks every TEMPLATE league's auction room, not only the new surface. *(**R584**: first written as 12, which counted 2 of SE.2's OWN new tests as pre-existing; verified against `main` by `grep`, corrected here and at three other sites. An inflated number inside a LAW breakdown is exactly what this file's §0 exists to stop.)* Recorded as **D268(6)**/**(11)**.**]** **Second probe for (6): feed the fork doc to `scoringFamilyFromRules` with the envelope-read removed and show the new pin RED returning `null` — the exact defect §0(A) predicts — then revert.** **[executed 2026-08-25 — 4 RED / 93 in that file (4 RED / 292 across the probe command), every failure reading `expected null to be 'standard'`/`'ppr'`; D268(8) probe C.]**

### SE.3 — The guardrail validator, TS side (F21's validator half)
> Read spec §7.3.3.1's F21-guardrails bullet **in full** (all five families incl. the format-1 arm of guardrail 2), §23.5's context list, D168, D146(1), F21's ledger row. Depends SE.1 + SE.2.
>
> 1. **`validateScoringRulesDoc(doc)`** in `src/lib/leagues/scoring/` — structured result (per-violation code + path + message; the settings-contract error shape). The five families: **(1) scorable-allowlist** — every `base` key and every override key ∈ the SE.1 scorable set (context raw sources/aggregates and reserved keys → named rejection); **(2) tier exclusivity** — format 2: `def_pa_*` keys ⊆ `tierKeysFromCuts('def_pa', doc.tier_cuts.def_pa)` and `def_ya_*` ⊆ the YA generation; format 1: `def_pa_*` ⊆ exactly one named family (shared 7-key XOR ESPN 8-key — the four shared names counted once), `def_ya_*` ⊆ the 9-key set; **(3) position scope** — `positions` keys ⊆ {QB,RB,WR,TE,K,DST}; an override under P references only P's section keys (K keys only under K, D/ST keys only under DST, offense keys never under K/DST); **(4) normal form** — no no-op overrides, no empty override objects; **(5) bounds** — finite, |coef| ≤ 100, multiples of 0.01. Plus `tier_cuts` residual validation (ascending integers, ≥ 2 cuts, per table, tables independent).
> 2. **A named rejection test per guardrail** (QA bullet) + the §4 rule 6 one-unit pins, incl. the F21 literal (`def_pa_14_20` + `def_pa_18_27` in one doc — the PA-18-20 double-pay — RED with a message naming tier exclusivity).
> 3. **Acceptance floor:** all 6 templates pass the format-1 arm; `forkTemplateDoc` of each passes the format-2 arm; a hand-built override doc (e.g. TE-premium-style `positions.TE.receptions` bump — legal) passes.
> 4. Zod envelope schema for the route layer (UX only — the spec's words: "client Zod is UX only"); it delegates semantic checks to the validator, no second implementation.
>
> DoD: plan §2.3; §4 rules 5/6/9; suites green; break probe — comment out the tier-exclusivity family and show exactly the named rejections for guardrail 2 (and no others) go RED, revert. F21's row is NOT flipped here (the law is server-side — SE.4).

### SE.4 — Migration A: `scoring_rules_validate` + `scoring_tier_keys_from_cuts` in SQL (the validator layer)
> Read spec §7.3.3.1(5), §7.3.8's v2.11 bullet (~~spec:436~~ **[corrected 2026-08-25 → `spec:439`; cite by its phrase "Exactly one scoring system referenced and readable by the league…"]**), D168/D175 + the §1 snapshot-writers survey **as corrected**, plan §8.1–8.3. Depends SE.3. ~~Migration number per D166 (expected **090**; pgTAP strictly above tasks-M3 §7:360's **032–038** reservations — expected **039**)~~ **[corrected 2026-08-25 — 090 and 039 are BOTH CONSUMED** (`090_snake_pause_first.sql` / `039_mock_cpu_reactive_bidding.sql`), and the 032–038 band is a dead letter. **Take the next free number measured at task time** — `ls supabase/migrations/ | tail -1` and `ls supabase/tests/ | tail -1`, heads **102 / 050** on 2026-08-25, so expect **103** and **pgTAP 051** — under D161's grep-together check across `main` and every open branch, with the confirming `ls` quoted in the migration banner.]
>
> 1. **`scoring_rules_validate(p_rules jsonb) RETURNS void`** — plain function (not SECURITY DEFINER — it reads no tables; literal allowlist + cut-list generation mirrored in SQL), `SET search_path = ''`, IMMUTABLE, RAISE P0001 with the guardrail family named in the message on first violation. Mirrors SE.3 family-for-family, incl. the format-1 arm (templates must pass — D175's wall and D168's backstop will both see format-1 docs) and the format-discriminator rule (any doc with a `format` member is format 2; `format` values other than 2 refuse loudly — forward-compat is a future validator version, never a silent pass).
> 2. **`scoring_tier_keys_from_cuts(p_prefix text, p_cuts jsonb) RETURNS text[]`** — the SQL twin of `tierKeysFromCuts`, used by (1); its own pgTAP pin regenerates the three families byte-exactly (the SE.1 pin, at the second layer).
> 3. **pgTAP at the function boundary:** every SE.3 named rejection reproduced (call the fn directly) + the D146(1) one-unit pins; all 6 seeded template rows' `rules` ACCEPT (read from the table, not re-authored); a fork-shaped format-2 doc ACCEPTS.
> 4. **TS≡SQL parity fixture (D168(1)):** one shared JSON fixture of accept/reject docs (≥ every named rejection + the acceptance floor) run through `validateScoringRulesDoc` AND `scoring_rules_validate`, verdicts asserted identical.
> 5. Migration banner: checklist §8.1 + R6/D38 waivers cited; typegen re-run (new PostgREST-visible function) with the alias-block rule.
>
> DoD: plan §2.3; §4 rules 1–6/9; fresh `npx supabase db reset` over the full chain SHOWN + `npm run test:db` + `npm run test` + `test:gate` + `type-check`; break probe — weaken the SQL bounds arm to `<= 101` and show exactly the |coef| one-unit pgTAP pins RED, revert. (F21 does NOT flip here — the walls are SE.4b's.)

### SE.4b — Migration B: the two write walls — the `scoring_systems` league-profile trigger + the `leagues` snapshot backstop (D175/D168; F21 flips here)
> Read **D175 in full** (Chris's ruling + the league-profile boundary, **including its added (6) — the mock reference path**), D168 **and its 2026-08-25 amendment**, §12.25's backstop sentence, §7.3.8's v2.11 bullet (~~spec:436~~ **[→ `spec:439`, cite by phrase]**), 059's D43 trigger (the pattern; cited in §1), plan §8.1–8.2. Depends SE.4. ~~Migration expected **091**~~ **[corrected 2026-08-25 — 091 is consumed (`091_mock_cpu_reactive_bidding.sql`, AP.3). Next free at task time; head was 102 / pgTAP 050, so expect 104 and 052 AFTER SE.4 takes its own — confirm with `ls`, never inherit.]**
>
> 1. **The table wall (D175):** `scoring_systems_rules_guard()` + BEFORE INSERT OR UPDATE trigger on `scoring_systems` — when NEW is in the **league profile** (`is_template = TRUE` OR `NEW.rules ? 'format'` OR live-league-referenced per D175(2)'s EXISTS) and `rules` is new or changed, `PERFORM scoring_rules_validate(NEW.rules)`. Plain trigger fn, `SET search_path = ''` with schema-qualified reads (it reads `public.leagues`, unlike 059's NEW-only guard). Triggers fire for every role including service_role (the D43 argument) — that is the point: **an invalid league doc cannot be written by ANY path**, including 001:623's owner FOR ALL.
> 2. **The snapshot wall (D168(2)):** `leagues_scoring_rules_valid()` + BEFORE INSERT OR UPDATE trigger on `leagues` validating any new/changed non-NULL `scoring_rules_snapshot`. Additive beside 059's `trg_leagues_snapshot_guard`; 059 is not edited (D137's no-in-place posture).
> 3. **Index:** `leagues.scoring_system_id` has no index anywhere in 001–087 (swept 2026-08-18 — `grep` over the migrations for an index naming the column hits nothing); add `idx_leagues_scoring_system`, since wall (1)'s reference arm and §12.25's member policy both scan `leagues` by it (plan §8.1's FK-index line). Confirm still absent at task time. **[re-measured 2026-08-25 — STILL ABSENT across 001–102**: `grep -rn "idx_leagues_scoring\|INDEX.*scoring_system_id" supabase/migrations/*.sql` returns nothing. Re-run it anyway — this is the §4 rule 9 register, not a licence to inherit.]
> 4. **pgTAP:** the FOR ALL bypass refused AT THE TABLE — as the authenticated owner, UPDATE an own league-referenced/format-2 row to each named-rejection doc → P0001 naming the guardrail (and a RETURNING-count proof the row is unchanged); a **research-profile write PASSES** (non-template, unreferenced, flat doc with legacy-shaped keys — the D33 protection, pinned); **[added 2026-08-25, per D175(6)] a MOCK-referenced write also PASSES** — a non-template row referenced only from `drafts.config->>'scoring_system_id'` (MP.4's path, which the profile's `EXISTS (… FROM leagues …)` arm cannot match) stays writable with a legacy-shaped flat doc, pinning the D33 protection at the second reference path too; template writes re-validate (all 6 seeded rules ACCEPT on a touch-write, read from the table); the leagues wall: privileged `UPDATE leagues SET scoring_rules_snapshot = <invalid>` refused even as postgres (triggers bind every role), valid doc lands; **the D175(4) profile census:** count rows matching the profile at build time (expected: exactly the template rows — state the number) and show each passes the validator.
> 5. **The bypass e2e, rewritten around the ruling (stack vitest):** as the commissioner's own client over 001:623's FOR ALL path, attempt to corrupt the league's fork row with an invalid doc → **the write itself is refused loudly** (Chris's sentence, proven at the table). Then prove the second wall INDEPENDENTLY by forging the state privileged — ~~`ALTER TABLE scoring_systems DISABLE TRIGGER` as superuser in-test (documented in the test as the D175 forge, **the only way the state can exist**)~~ **[corrected 2026-08-26 by the SE.4b Builder — IT IS NOT THE ONLY WAY, and the other way needs no DDL, no superuser and no `docker exec` from a vitest process. Measured and shipped instead (PROGRESS D272(6), ledger F142): (a) the owner writes the corrupt FLAT document to a row of their own WHILE IT IS UNREFERENCED — legal, and it must stay legal, because that is a D33 research row at that moment and 104's profile deliberately does not reach it; then (b) `leagues.scoring_system_id` is REPOINTED at it — a write to `leagues`, which wall 1 does not guard and wall 2 does not guard either (a repoint does not change the snapshot). This forges the state THE WAY IT CAN ACTUALLY ARISE, which makes the proof stronger rather than weaker: wall 1 never fires in that act at all, so wall 2's refusal is genuinely independent of it. Step (b) is privileged-only today because 061 is still template-only — which is exactly the door D169's attach arm closes, so this correction is also the measurement of why SE.5 still owes it.]**, write the corrupt doc, re-enable — and show `draft_start` refuses at the snapshot wall (league stays `scheduled`, snapshot stays NULL); fix the doc → `draft_start` succeeds and the snapshot deep-equals the doc **verbatim** (jsonb equality — the §7.3.3.1 freeze-verbatim pin).
> 6. Migration banner per §8.1 with waivers; typegen only if the PostgREST surface changed (trigger fns are not exposed — say so in the banner if skipped).
>
> DoD: plan §2.3; §4 rules 1–6/9; fresh reset SHOWN + full suites; break probe — drop the reference arm from the profile (leaving format-only) and show the corrupt-flat-while-attached pgTAP pin go RED at its exact count, revert. **Flip F21 → Done in the same PR** (pointer to SE.3 + SE.4 + SE.4b; SE.5 completes the attach surface — reviewers audit).

### SE.5 — Migration C: `scoring_fork_template` + `scoring_update_rules` + the §12.25 SELECT policy + the 061 attach amendment
> Read spec §12.25 **in full** (policy SQL is printed there — use it verbatim; **the header is now `spec:1487`, not `spec:1423`**), §7.3.3.1's entry-point + lifecycle bullets, §7.3 header (spec:197 — the `setup`/`scheduled` window; **re-measured 2026-08-25, still lands**), D169/D170/D175, 061's step-5/step-6 bodies, D59's ownerless CHECK, plan §8.2–8.3. Depends SE.4b. ~~Migration expected **092**.~~ **[corrected 2026-08-25 — 092 is consumed (`092_auction_reserve_toggle.sql`, AP.1). Next free at task time, after SE.4 and SE.4b take theirs; heads were 102 / 050, so expect 105 and pgTAP 053. Confirm with `ls`.]** **[re-measured 2026-08-26 — SE.4 took 103/051 and SE.4b took **104/052**, so the heads ARE now 104 / 052 and the 105 / 053 expectation holds. It is still an expectation: measure with `ls` at task time (D166/D161), because the number taken out from under this lane every single time is the one a planning document wrote down.]**
>
> 1. **`scoring_fork_template(p_league_id uuid, p_template_id uuid)`** — SECURITY DEFINER, `search_path=''`, in-body commissioner check (42501), league not soft-deleted and status IN ('setup','scheduled') (P0001 naming the §7.3 window), `p_template_id` must be a template row (the 060/061 predicate); INSERT the custom row (`owner_id` = the commissioner, `is_template = FALSE`, `name` = `<league.name> Custom`, `rules` = the format-2 fork doc built in SQL — `base` verbatim from the template + inherited `tier_cuts` per SE.2(4)'s family-detection rule) + repoint `leagues.scoring_system_id`, one transaction; `PERFORM scoring_rules_validate` on the doc before insert (defense in depth; the trigger is the backstop, the RPC is the front door). Returns the new row id. **Idempotency (plan §2.3):** if the league's current reference is already a non-template row owned by this commissioner whose `rules` deep-equal the would-be fork doc, return that id without inserting (retry-safe by natural key; a deliberate re-fork after edits creates a fresh row and orphans the old one — §12.25's accepted hygiene).
> 2. **`scoring_update_rules(p_league_id uuid, p_rules jsonb)`** — same auth form + window; refuses when the league's referenced row is a template (P0001: "fork first — templates are immutable, §7.3.3.1") or not commissioner-owned; `PERFORM scoring_rules_validate(p_rules)`; writes `normalizeScoringDoc`'s output? **No — normalization is the CLIENT's save-time duty (SE.7); the RPC validates guardrail (4) and refuses un-normalized docs** (one canonical byte-shape in the DB; a server that silently rewrites what it was sent is the "nothing happened" class). Naturally idempotent (same doc → same row state).
> 3. **The §12.25 member SELECT policy, verbatim** ~~from spec:1429–1434~~ **[⚠ CORRECTED 2026-08-25 — THE HIGHEST-RISK CITATION IN THIS DOCUMENT. `sed -n '1429,1434p' docs/specs/spec-redraft-leagues.md` today returns `CREATE INDEX idx_team_managers_league …` and `CREATE POLICY "Stints viewable by league members"` — A DIFFERENT TABLE'S RLS POLICY. A "copy this verbatim" instruction pointed at plausible, syntactically valid, completely wrong SQL, and a Builder following it would have shipped a `team_managers` policy onto `scoring_systems`. THE POLICY IS AT `spec:1493–1498` — but do not copy by line number: find it by its NAME,** `CREATE POLICY "League members read league scoring" ON scoring_systems FOR SELECT`, and copy from there to its closing `));`. Verified byte-unchanged from the v2.11 text. **This is §4 rule 9's "cite by symbol" rule earning its place — the one instruction in this file that could have produced a wrong-outcome bug from correct-looking obedience.]** pgTAP: a member of a league referencing the row SELECTs it; a non-member cannot; the moment the league repoints away, the same member's SELECT stops matching (the unreferenced-orphan property); templates stay world-readable; **no-write-policy pattern per role with RETURNING counts** — the new policy grants SELECT only: member (non-owner) UPDATE/DELETE on the custom row = 0 rows; anon everything = 0 rows; the D59 ownerless CHECK still refuses minting templates (fork RPC cannot set `is_template`, pinned).
> 4. **061 amendment (D169):** `CREATE OR REPLACE FUNCTION update_league_settings` **authored against the chain HEAD body** (CLAUDE.md's hotfix-at-HEAD rule — read the newest migration defining it, confirm at task time nothing above 061 replaced it; §1's recorded sweep — `grep -rln "FUNCTION update_league_settings" supabase/migrations/` → 061 only — verified none through 087; **[re-measured 2026-08-25: still 061 ONLY through 102]** — and note that `draft_start_internal`, swept in the same §1 pass, moved TWICE in the same window, so run the grep rather than trusting either sweep). Step 5 gains the "OR the league's own current reference" arm **plus the D169 attach-validation arm: whenever `p_scoring_system_id` is passed, `PERFORM scoring_rules_validate` on the referenced row's `rules` before the write (spec:347's settings-attach MUST — step 6's re-freeze never fires on a same-row re-attach, so this is the attach path's OWN check: defense-in-depth over D175's wall and the user-facing error surface, per D169)**; everything else byte-identical. pgTAP: **the attach-arm pin — with D175's wall in place a bypass-corrupted row cannot exist, so the pin FORGES the state privileged (the SE.4b(5) mechanism: superuser-disable the scoring_systems trigger in-test, write the corrupt doc, re-enable) and then shows the re-attach refuse P0001 from the attach arm itself** — proving this layer works independently of the wall; re-attach of the valid own fork row accepts; another user's personal row still refuses; a template still accepts (and detaches the fork by repointing); the step-6 re-freeze of a format-2 doc lands and the backstop trigger sees it (already covered by SE.4b's trigger pins — cite, don't re-prove).
> 5. Typegen (two new RPCs) with the alias-block rule.
>
> DoD: plan §2.3; §4 rules 1–6/9; fresh reset SHOWN + full suite run; break probe — remove the fork RPC's status-window check and show the pgTAP window pins RED (a `drafting` league forking), revert.

### SE.6 — Routes + hooks (+ the §15.1 route erratum)
> Read **§0(B) and §0(C) FIRST** (the hook already ships; the invalidation hole), then spec §15.1 (~~spec:1494~~ **[corrected 2026-08-25 → `spec:1581`; cite by its header `### 15.1 League & membership`]**) + the C40/v2.8.17 route-erratum precedent (tasks-M3 §9 C40), §12.25's write law, SE.3's Zod schema, the CLAUDE.md route pattern. Depends SE.5. No DB surface.
>
> 1. **`POST /api/leagues/[id]/scoring/fork`** `{ template_id }` → `scoring_fork_template`; **`PUT /api/leagues/[id]/scoring/rules`** `{ rules }` → `scoring_update_rules`. Zod-parse (SE.3's schemas — UX-layer only), auth via the house server-client pattern, RPC errors mapped to structured field errors (the settings-panel `LeaguePatchError` shape); the P0001 messages surface verbatim (they were written to be user-readable).
> 2. ~~**Reads need no new route:** `use-league-scoring.ts` hook — query keyed on the league's `scoring_system_id`, reading `scoring_systems` over the §12.25 member policy (client Supabase read, the `useScoringTemplates` pattern); mutations for fork/save with invalidation of the league detail + the scoring query. One hook file (house rule).~~
>
>     **[REWRITTEN 2026-08-25 — THIS HOOK ALREADY SHIPS. Read §0(B)/§0(C).]** `useLeagueScoringFamily` landed in **`src/hooks/use-draft-pool.ts`** on **2026-08-20** (`4e7170a` @ `2026-08-20T02:03:24-07:00`, L.C3.3 #177) — **16h23m after this breakdown MERGED** (#161 @ `2026-08-19T09:40:38-07:00`), **35h25m after it was written** (`90cb5a8` @ `2026-08-18T14:38:34-07:00`) — and was extended by MP.6c (`66c415b`, #199). It already does most of what item 2 described: it reads `leagues.scoring_rules_snapshot`, falls back to `leagues.scoring_system_id`, and resolves either through `scoring_systems.rules` by id. **Writing `use-league-scoring.ts` beside it would be the near-duplicate CLAUDE.md forbids.**
>     - **(a) State the disposition explicitly in the PR body and the hook's docblock: REPLACES, WRAPS, or COEXISTS with `useLeagueScoringFamily`.** CLAUDE.md's no-near-duplicate rule points at **wrap-or-extend**; a genuine third option is fine, but it must be argued, not defaulted into. Whichever you choose, there is **one** hook that reads a league's scoring document at the end of this task.
>     - **(b) THE INVALIDATION LIST MUST NAME `auctionPoolKeys.scoring(leagueId)`** — defined in `use-draft-pool.ts` as `['league-scoring-family', leagueId]` with **`staleTime: 10 * 60 * 1000`**. `grep -rn "auctionPoolKeys.scoring\|league-scoring-family" src/` → **exactly 2 hits: the definition and the one use. NOTHING INVALIDATES IT TODAY.** A fork or a save that does not invalidate it leaves an open auction room serving **pre-edit scoring for up to ten minutes**, silently and with no error — CLAUDE.md's *"never let 'nothing happened' mean 'it worked'"* failure shape. **Pin it:** a test that the fork mutation and the save mutation each invalidate that exact key (assert on the key, not on a refetch count).
> 3. **Spec erratum (Builder-carriable, the fold-back rule):** §15.1 gains the two routes, citing C40's precedent. ~~with a one-line v2.11.x changelog entry … version header bumped~~ **[corrected 2026-08-25 — THE ORIGINAL INSTRUCTION IS UNEXECUTABLE.** `grep -c '^- \*\*v2\.11\.' docs/specs/spec-redraft-leagues.md` → **0**: no v2.11.x band was ever minted, and **23 changelog entries now sit above v2.11**. Minting a v2.11.x entry would *lower* the version header from its current **2.16.6**. **Do this instead: add a one-line PATCH entry at the next free version ABOVE the changelog head at build time — v2.16.7 as of 2026-08-25 — and confirm the header with `sed -n '4p' docs/specs/spec-redraft-leagues.md` before you write it.** Precedent measured: **AP.6's own erratum folded as v2.16.5 — head-adjacent, not into its lane's original band.** Take the next free **E** number under D161 at the same time (main's high-water was **E80** on 2026-08-25, so expect **E81** — confirm). **The erratum is still genuinely owed:** `grep -n 'scoring/fork\|scoring/rules' docs/specs/spec-redraft-leagues.md` → no hits; §15 carries no scoring routes.]
> 4. Service-layer functions colocated per the draft-service pattern; no client computes or rewrites a rules doc outside `normalizeScoringDoc`.
>
> DoD: plan §2.3; §4 rule 9; route tests (auth refusal, Zod refusal, happy path against the local stack); `npm run test` + `type-check` + `lint`; break probe — point the fork route at a non-commissioner session and show the 42501 → 403 mapping test RED when the in-route auth guard is removed, revert.

### SE.7 — The editor shell: `scoring-editor-ops.ts` + `scoring-editor.tsx` (stepper, five sections, All-Positions switch)
> Read spec §7.3.3.1's product-shape + editable-scope + All-Positions bullets, §16.2 (~~:1625–1626~~ **[corrected 2026-08-25 → `spec:1718–1719`; cite by the rows keyed on `scoring-editor.tsx` and `scoring-editor-ops.ts`]**), §16.5.4 (required states), D167/D173, CLAUDE.md design rules + elevation ruling, SE.2/SE.3/SE.6 exports. Depends SE.6.
>
> 1. **`scoring-editor-ops.ts`** (pure; composes the engine per D167): `SECTION_CATALOG` — the §7.3.3.1 pinned catalog verbatim: five offense sections with per-position key scoping, Kicking (K), D/ST (events + tier tables), **`return_yards` present with `gated: true`** (D173 — renders nothing while gated; the catalog is the only place the field exists); `sectionsForPosition(P)` (QB→RB→WR→TE five-section pages; K and DST one single-section page each — Q12 ruled); `allPositionsOn(doc, section)` — **derived state: ON iff none of the section's keys carry a position override** (§7.3.3.1 verbatim; pin the truth table incl. the mixed case); `applyEdit(doc, {section, key, value, scope})` — switch-ON edits write `base`, position edits write `positions[P]`, output normalized via `normalizeScoringDoc`; per-field input rules (step 0.01, |100| clamp-with-error per guardrail 5 — client mirrors, server law).
> 2. **`scoring-editor.tsx`** — the position stepper QB → RB → WR → TE → K → D/ST (§7.3.3.1's order, literal); per-section value grids; the All-Positions switch per section; save through the SE.6 mutation with server-refusal rendering (never a silent revert); commissioner-only edit affordance, `setup`/`scheduled` only (the RPC enforces; the UI states it). §16.5.4's four states designed (skeleton/empty/error-with-retry/degraded); mobile/tablet/desktop.
> 3. Ops pins: catalog-completeness ≡ SE.1's scorable set minus gated minus the tier-indicator keys (which render as the D/ST tier tables, not grid rows — SE.8); switch-derivation goldens; `applyEdit` arm goldens; a **source-level pin that no ungated `return_yards` row can render** (the launch-scope-gates sweep idiom).
> 4. Sample-line strip mounts here as a placeholder region only — SE.8 fills it (say so in a comment naming SE.8).
>
> DoD: plan §2.3; §4 rules 7/9; ops suites green; browser pass at three widths (R278 prose + DB corroboration — an edit lands in `scoring_systems.rules` and reads back); break probe — force `allPositionsOn` to ignore overrides and show the truth-table goldens RED, revert. One review pass (UI); ops findings are findings.

### SE.8 — K + D/ST pages + the six live sample lines through the REAL pipeline
> Read spec §7.3.3.1's sample-players table (the six pinned lines — stored literals), the D/ST-tiers bullet (b) (inherited boundaries, read-only), Q12's ruling text in PROGRESS §3, D171, D62 (hand-computed literals), F59's ledger row. Depends SE.7.
>
> 1. **K page:** the Kicking table as one single-section page (Q12). **D/ST page:** the seven event categories + the doc's tier tables — **PA and YA are independent, additively-paying tables** (the ruled correction — never render them as alternatives); table presence = what the doc pays (an ESPN fork shows PA + YA; a shared-family fork shows PA only); **tier ROW LABELS are generated from the doc's own `tier_cuts` via `tierKeysFromCuts`** — never hand-written ranges — and boundaries render as **read-only labels: only what each tier PAYS is editable** (~~§16.2:1625's~~ **[corrected 2026-08-25 → `spec:1718`, the `scoring-editor.tsx` row]** exact words).
> 2. **The six sample lines as stored literals** (§7.3.3.1's table, verbatim — Marino/Peterson/Moss/Gronk/Rackers/Seahawks): delivered stats only; context numbers rendered as flavor, omitted from the stat objects (D171); the Seahawks line stores RAW `def_points_allowed: 17` / `def_yards_allowed: 289` and the tiers one-hot through `deriveTierIndicators` — the 17 PA deliberately straddles the families (ESPN pays `def_pa_14_17`, shared pays `def_pa_14_20`; YA lands `def_ya_200_299`) and the page must make that visible, per the spec's own note.
> 3. **Live total = `scorePlayerWeek(resolveRules(doc, P), deriveTierIndicators(line))`** recomputed on every edit — the REAL pipeline, no parallel math (D171). **Literal pins (D62): all six sample players' totals hand-computed and stored as literals** under (a) at least two template forks (one ESPN-family, one shared-family — the D/ST line must pin DIFFERENT totals under the two families' cuts) and (b) one override doc exercising a per-position edit that changes exactly one player's total.
> 4. **The F59 gate, pinned:** a source-level sweep pin that the editor renders **zero boundary inputs** — no `tier_cuts` field is writable from any component (grep-style, the no-second-door idiom); F59's row is where boundary editing lives, explicitly not here.
>
> DoD: plan §2.3; §4 rules 5/6/7/9; the six literal pins green; browser pass (R278 form) showing a K edit and a D/ST tier-pay edit land and the sample totals move; break probe — hand-one-hot the Seahawks tiers (bypassing `deriveTierIndicators`) and show the D/ST literal pins RED under the shared family, revert. One review pass (UI); pins are pins.

### SE.9 — "Customize" entry + the fork flow + the member read-only view
> Read spec §7.3.3.1's entry-point + access bullets, §12.25's orphan-hygiene bullet, D169/D170, the picker's docblock (~~consumed by wizard AND settings~~ **[corrected — THREE mounts; see §1's corrected picker bullet and (1) below]** — never fork it), SE.6's hooks. Depends SE.6 + SE.7.
>
> 1. **The picker gains "Customize"** (~~§16.2:1624~~ **[corrected 2026-08-25 → `spec:1717`, the `scoring-template-picker.tsx` row]**) behind a league-context prop (D170): visible only in the settings surface for a commissioner with the league in `setup`/`scheduled`; the wizard's picker is byte-identical in behavior plus at most the D170 hint line.
>     - **[corrected 2026-08-25 — THERE ARE THREE MOUNTS, NOT TWO, AND THE THIRD HAS NO LEAGUE.** `grep -rn "ScoringTemplatePicker" src/ | grep -v scoring-template-picker` → `league-create-modal.tsx:268`, `settings-panel.tsx:481`, and **`src/components/draft/mock-launch-dialog.tsx:286`** (MP.4, `6e9e8d1`, 2026-08-24), which passes only `value`/`onChange` — a standalone mock has no league (`drafts.league_id` is nullable, migration 095). **The league-context prop MUST default to "no league" so the mock mount is unchanged, and all THREE mounts are pinned, not two.** Fix the picker's own docblock in the same PR: it still says "BOTH".]
>     - ~~pinned — the existing picker test's card-count/selection pins stay green unmodified~~ **[corrected 2026-08-25 — THAT PIN DOES NOT EXIST.** `grep -c 'render(\|@testing-library' src/components/leagues/scoring-template-picker.test.ts` → **0**. The file is pure-ops: it has no render assertions at all and **cannot** catch a Customize affordance leaking into the wizard or the mock launcher. **SE.9 owes a REAL render pin** — mount the picker in each of the three configurations and assert Customize is present in exactly one (settings, commissioner, `setup`/`scheduled`) and absent in the other two. Per §4 rule 9, show it RED against a deliberately leaked affordance before you count it as a pin.] Customize on a template card → fork mutation → the editor opens on the new doc. Re-forking from a different template detaches the old row (the repoint — §12.25; surface the "your previous customizations remain saved but unattached" copy per §16.5.4's designed-copy rule).
> 2. **Re-picking a plain template** through the settings panel keeps working against the amended 061 (SE.5) — verify and pin the round trip: template → fork → edit → back to template → the member view shows the template again.
> 3. **Member read-only view (§7.3.3.1 access bullet):** league members see the custom rules pre-draft on the settings surface (the §12.25 SELECT policy feeds it) — read-only rendering of the doc (sections + per-position overrides + tier tables), no edit affordances, no new feed. Post-draft the surface reads the snapshot (existing law; state it in the component docblock).
> 4. States per §16.5.4; the "commissioner in `drafting`+" arm renders the read-only view with the "scoring locked at draft" line, not a disabled editor.
>
> DoD: plan §2.3; §4 rules 7/9; browser pass (R278 form) of the full loop in 2 sessions (commissioner forks+edits, member sees it read-only; DB rows corroborate); break probe — render Customize for a member session and show the commissioner-only pin RED, revert. One review pass (UI).

### SE.10 — The editor's quality gate: the §7.3.3.1 QA suite, composed and shown
> Read spec §7.3.3.1's QA-matrix bullet (the enumerated suite), D172, every prior SE task's pins, plan §2.3. Depends everything.
>
> 1. **Enumerate and run the QA-bullet suite item-by-item with counts:** resolver identity (format 1 ≡ resolved, every position) · fork-equivalence (all 6 templates) · a named rejection per guardrail (TS and SQL) · sample-line literal pins for all six players · the cut-list ≡ key-names equivalence for all three families · the derive-agreement invariant · the invalid-doc draft-start refusal · **the D175 wall refusals + the research-profile pass (SE.4b — the ruling's own suite, beyond the spec's list)** · **[added 2026-08-25] the SE.2(6) envelope-read pin (a forked ESPN doc classifies to the same family as its template — F133) and SE.9's three-mount Customize render pin**. Each mapped to its suite + assertion count in the PR body (rule 9: measured, not recalled).
> 2. **The byte-identity/zero-outcome-change evidence:** `template-parity.test.ts` + `templates-db.test.ts` green **unmodified** (show `git diff --stat` empty for both), the SE.1 regeneration pins green, the SE.2 identity property green — stated together as the track's headline proof.
> 3. Fresh `npx supabase db reset` over the FULL chain + `npm run test:db` + `npm run test` + `npm run test:gate` + `type-check` + `lint`, one run, output shown.
> 4. **Browser pass (R278 form) of the whole cohort story:** create league → pick template → Customize → per-position edits with live sample totals → member read-only view → draft start freezes the doc verbatim (snapshot jsonb-equality corroborated in the DB) → a deliberately corrupted doc refuses draft start loudly → fixed → drafts.
>     - **[ADDED 2026-08-25 — THE BROWSER PASS MUST ENTER THE AUCTION ROOM. Ledger F133; read §0(A).]** After draft start on the **customized** league, **open the auction draft room and read the player table's `Proj` column** — with `Pts/wk` and `$/pt` beside it. All three must show **numbers**, and the room must **not** render *"Scoring not readable — projections hidden."* This is the one observation that distinguishes SE.2(6) landing from SE.2(6) being skipped: **the editor's own sample-line totals stay correct either way**, because they run the resolver, while the auction room reads the raw document and would go blank. A gate that only reads sample lines is green through the defect — which is exactly how this survived to 2026-08-25 unnoticed.
>     - Corroborate in the DB that the league's `scoring_rules_snapshot` is the format-2 document (it is what the room's reader is being handed), and say so in the PR body with the query.
> 5. **PROGRESS:** every SE box checked; §1/§2 gate statement recorded — *"the editor's own quality gate (spec §7.3.3 v2.11) passed <date>"* — which is the fact Chris operates on for the GA-posture decision (D172); session-log row.
>
> DoD: plan §2.3; §4 rule 9 end-to-end (this task is mostly rule 9); no break probe of its own — it re-runs the track's existing probes' pin suites and reports their counts.

---

## 6. Ledger dispositions (every §6 row naming this track, swept 2026-08-18)

| Row | Disposition here |
|---|---|
| **F21** (custom-editor PA double-pay footgun, R55) | **Discharged by SE.3 (TS validator) + SE.4 (the SQL validator) + SE.4b (the D175/D168 write walls — where the row FLIPS to ✅), with SE.5 completing the attach surface**. The F21 literal (`def_pa_14_20` + `def_pa_18_27`) is a named one-unit rejection pin at every layer (§4 rule 6). This PR amends the row's "Discharged by" cell to match. |
| **F59** (D/ST tier-boundary editing) | **Stays OPEN — explicitly NOT in this track** (§7.3.3.1(b); the row's own routing). SE.1 lands `tierKeysFromCuts` and the derive cuts parameter (the machinery F59's row names), SE.8 pins that zero boundary inputs exist; the editing surface is a **follow-up Architect breakdown after SE.10**, which inherits the ruled cut-point input design (§7.3.3.1(c)). |
| **F71** (NEW — filed by this PR) | `return_yards` lights up when a data source is found: the §7.3.3.1 4-step checklist (adapter mapping → one `STAT_KEYS` entry with `scoring_surface: 'scorable'` → flip SE.7's catalog gate → done; no migration, no engine change). Discharged by that future standalone data-task PR (the C43 parallel-track pattern); SE.7's `gated: true` catalog entry is the receiving seam. |
| **F24** (Sleeper values best-known, unverified) | Untouched. A Sleeper-template fork inherits best-known values into `base` — acceptable (the commissioner is editing them anyway); the September verification updates TEMPLATES only and never rewrites a fork (forked docs are the commissioner's). Noted so nobody "fixes" forks retroactively. |
| **F10** (September raw-endpoint inspection) | Untouched — its lift path (60+ FG, PAT detail) would arrive as new registry keys via §23.5; the editor's catalog rendering is key-agnostic and receives them like any other. |
| **F133** (NEW — filed by the 2026-08-25 amendment) | **The format-2 envelope must not blind the auction room's projections.** `scoringFamilyFromRules` (`src/components/draft/auction-player-table-ops.ts`) reads a **top-level** `receptions` key; a format-2 doc has none, so the room would render *"Scoring not readable — projections hidden"* for every customized league from the first fork onward. **Discharged by SE.2(6)** (the envelope-aware read + its pin) **with SE.10(4)'s auction-room browser observation as the acceptance evidence.** Filed with its own row per R51 so the hand-off survives the next drift of this file. Full measurement chain: **§0(A)**. |
| F68 / F69 / F70 | Not this track's (DR/E2E, 088's broadcast, the unexplained vitest red) — listed to show they were swept, not missed. **[2026-08-25: not re-swept — five lanes of ledger rows have landed since this table was written. Re-sweep §6 of PROGRESS at task time; this table is a 2026-08-18 snapshot.]** |

---

## 7. Sequencing & lane rules (the parallel-track law)

1. **SE never blocks and is never blocked by `DR.*` or `L.C*`** (D166). The loop's precedence after this PR merges: DR.* → L.C* → SE.* (when both are blocked, or as Chris directs — "build SE.x" is always valid). ACTIVE-BUILD.md carries the operative text.
2. **The only shared surface is `PROGRESS-leagues.md`** — append-collisions resolved at merge (the established pattern); ledger/decision/review NUMBERS are assigned under the D161 rule: grep R, D and F together across `main` AND every open branch before claiming.
3. **Schema lane serialized within SE** (SE.4 → SE.4b → SE.5), ~~migrations strictly above tasks-M3 §7:360's 083–089 band and pgTAP strictly above its 032–038 band … expected 090/091/092 and pgTAP 039+; L.C1.8's floating "088+ next-free" claim (§1) can consume numbers too~~ **[corrected 2026-08-25 — all four expected numbers were consumed (090 by L.C1.8, exactly the floating claimant the struck text named; 091/092/pgTAP 039 by AP); both reservation bands are dead letters 13 numbers behind the heads; the sentence cited is at `tasks-M3 §7:361`, not `:360`. Heads on 2026-08-25: migration **102**, pgTAP **050**. **Measure at task time with `ls`; expect 103/104/105 and 051+.**]**, so confirm at task time. Any `CREATE OR REPLACE` (SE.5's 061 amendment is the only one in this track) is authored against the chain HEAD at build time, with the confirming grep in the migration banner.
4. **Within-track order is the dependency graph in §5.** SE.1–SE.3 are pure TS and are the right tasks to take when the schema lane is contended; SE.4 is the track's first migration and its Builder re-confirms both chain heads.
5. One task, one branch, one PR; the orchestrator merges; PROGRESS updated in the same PR (checklist box, D-entries for build mechanics, ledger flips, session-log row) — the standing law, restated once.

---

## 8. Open items & notes (not tasks)

- **The Q13 `return_yards` checklist (a note, deliberately not a task):** when a source is found — (1) map the feed field in the provider adapter into `player_stats.advanced` (no migration, §23.5's storage rule); (2) add one `STAT_KEYS` entry (`return_yards`, tier `core_box`, `storage: 'advanced'`, `scoring_surface: 'scorable'`); (3) flip the SE.7 catalog gate ON; (4) done — calculator, resolver, guardrails, and editor rendering are key-agnostic. Tracked as **F71**; runs as its own small data-task PR in parallel with anything (the C43 pattern), never as an SE dependency.
- **Sample-line copy is Chris-amendable on THIS PR** (§7.3.3.1's own note: "the numbers above are product voice — Chris may amend them on this entry's PR without re-opening the mechanism"). If he edits the lines here, SE.8's literals follow the merged spec, and the hand-computed pins are computed from whatever merges. **[corrected 2026-08-25 — "THIS PR" (#161) merged 2026-08-19 unamended, so this window has CLOSED and the six lines as printed in §7.3.3.1 are what SE.8 pins.** The underlying note still stands for any future amendment: the numbers are product voice and Chris can change them without re-opening the mechanism — but that would now be its own spec change, not an edit to an open breakdown PR.]
- **D170 (Customize only in league context; wizard stays templates-only) is flagged for Chris's read** — ~~he holds this PR open, which is the right vehicle~~ **[corrected 2026-08-25 — PR #161 merged 2026-08-19 with D170 as written and no comment, so D170 STANDS as approved.** A reversal is still a small change to SE.9's scope (a post-create redirect flow) and no schema task, but it would now arrive as a fresh ruling, not as a comment on an open PR. **Note for whoever revisits it: the picker now has a THIRD, league-less mount (the MP.4 mock launcher) — any "Customize in the wizard" reversal has to say what that mount does, which was not true when D170 was written.**]
- **D174 (`def_pa_0` floor vs "first open-below")** is a resolved spec-text tension recorded with its resolution (the spec's own no-behavior-change clause controls); if Chris reads it differently, SE.1 is the only task affected and it has not started.
- **Post-track follow-ups already routed:** F59's boundary-editing breakdown (after SE.10); the M4 scoring fan-out consumes `resolveRules` from the engine home (D167) — noted in tasks-M4's future survey, not owed by SE.
- **`scoring_update_rules` refuses un-normalized docs rather than normalizing server-side** (SE.5(2)) — if cohort feedback shows clients other than ours writing docs (there are none today), revisit as a v-next decision; the refusal message names `normalizeScoringDoc`.
