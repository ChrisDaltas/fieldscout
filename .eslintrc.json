{
  "extends": ["next/core-web-vitals", "next/typescript", "prettier"],
  "overrides": [
    {
      "files": ["src/lib/leagues/**", "src/lib/sync/live-stats.ts", "src/lib/sync/ingest-week.ts", "src/lib/sync/ingest-flags.ts", "src/lib/sync/live-poll.ts"],
      "rules": {
        "no-restricted-syntax": [
          "error",
          {
            "selector": "CallExpression[callee.object.name='Date'][callee.property.name='now']",
            "message": "League-engine code reads time from an injected TimeProvider, never Date.now() (spec §23.1/§23.6; M0 D3). Use time.now().getTime()."
          },
          {
            "selector": "NewExpression[callee.name='Date'][arguments.length=0]",
            "message": "League-engine code reads time from an injected TimeProvider, never `new Date()` (spec §23.1/§23.6; M0 D3). Use time.now()."
          },
          {
            "selector": "CallExpression[callee.name='Date']",
            "message": "Bare Date(...) always returns the current wall time regardless of arguments (D3/R15). Use the injected TimeProvider."
          },
          {
            "selector": "CallExpression[callee.object.name='Date'][callee.property.value='now']",
            "message": "Date['now']() is a wall-clock read (D3/R15). Use the injected TimeProvider."
          },
          {
            "selector": "MemberExpression[object.name='globalThis'][property.name='Date']",
            "message": "globalThis.Date routes around the time-guard (D3/R15). Use the injected TimeProvider."
          },
          {
            "selector": "CallExpression[callee.object.name='performance'][callee.property.name='now']",
            "message": "performance.now() is a wall-clock read (D3/R15). Use the injected TimeProvider."
          },
          {
            "selector": "MemberExpression[object.name='globalThis'][property.name='performance']",
            "message": "globalThis.performance routes around the time-guard (D3/R15). Use the injected TimeProvider."
          },
          {
            "selector": "VariableDeclarator[init.name='Date']",
            "message": "Aliasing the Date constructor routes around the time-guard (D3/R15). Use the injected TimeProvider."
          },
          {
            "selector": "AssignmentExpression[right.name='Date']",
            "message": "Aliasing the Date constructor routes around the time-guard (D3/R15). Use the injected TimeProvider."
          },
          {
            "selector": "NewExpression[callee.name='Date'] > SpreadElement",
            "message": "new Date(...spread) can be an argless wall-clock read at runtime (D3/R15). Pass explicit arguments or use the injected TimeProvider."
          },
          {
            "selector": "CallExpression[callee.object.name='Math'][callee.property.name='random']",
            "message": "League-engine code uses the seeded deterministic PRNG (synthetic/prng.ts), never Math.random (spec §23.6; L.A0.3, D3 spirit)."
          },
          {
            "selector": "CallExpression[callee.object.name='Math'][callee.property.value='random']",
            "message": "Math['random']() routes around the determinism guard (L.A0.3, D3 spirit). Use the seeded PRNG."
          },
          {
            "selector": "MemberExpression[object.name='globalThis'][property.name='Math']",
            "message": "globalThis.Math routes around the determinism guard (L.A0.3/R22). Use the seeded PRNG (synthetic/prng.ts)."
          },
          {
            "selector": "VariableDeclarator[init.name='Math']",
            "message": "Aliasing (or destructuring) the Math object routes around the determinism guard (L.A0.3/R22). Use the seeded PRNG."
          },
          {
            "selector": "AssignmentExpression[right.name='Math']",
            "message": "Aliasing the Math object routes around the determinism guard (L.A0.3/R22). Use the seeded PRNG."
          },
          {
            "selector": "CallExpression[callee.object.name='crypto'][callee.property.name='getRandomValues']",
            "message": "crypto.getRandomValues() is a nondeterministic random source (L.A0.3/R22). Use the seeded PRNG (synthetic/prng.ts)."
          },
          {
            "selector": "CallExpression[callee.object.name='crypto'][callee.property.value='getRandomValues']",
            "message": "crypto['getRandomValues']() routes around the determinism guard (L.A0.3/R22). Use the seeded PRNG."
          },
          {
            "selector": "MemberExpression[object.name='globalThis'][property.name='crypto']",
            "message": "globalThis.crypto routes around the determinism guard (L.A0.3/R22). Use the seeded PRNG."
          }
        ]
      }
    }
  ]
}
