/**
 * TEMPORARY UN-GATED VERSION — the RED half of MP.6's gate pin. Replaced by
 * the real gate in the next commit.
 */
export default function MockRoomGate({
  children,
}: {
  children: React.ReactNode
}) {
  return <>{children}</>
}
