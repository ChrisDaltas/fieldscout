# Migrations deliberately NOT yet applied to production.
#
# WHY THIS FILE EXISTS
# The drift check (.github/workflows/db-drift.yml) exists because on 2026-08-05
# production was found missing THIRTY-SIX migrations, discovered only because a
# signup broke. From 2026-08-13 the check went red again and STAYED red for
# twelve days — not because anything was wrong, but because the leagues schema
# is deliberately held back until the 2026 invite-only test cohort. A permanently
# red alarm cannot alarm. This file is how "deliberately held" is SAID OUT LOUD,
# so the check can go green and start distinguishing again.
#
# RULES THAT KEEP THIS FILE FROM BECOMING THE PROBLEM IT SOLVES
#  1. Ranges are CLOSED. There is no "082+". A new migration falls OUTSIDE every
#     hold by default and reds the check until someone deliberately either pushes
#     it or adds it here. That default is the whole point.
#  2. A hold naming a migration that IS applied to production is STALE and fails
#     the check. Holds expire loudly, not silently.
#  3. A hold naming a migration that is not in the repo is ROT and fails the check.
#  4. Nothing here suppresses a STRAY — a version applied to production that the
#     repo does not have. That is the hand-applied-migration direction that caused
#     the 2026-08-05 incident and it always fails.
#
# FORMAT: one NNN or NNN-MMM per line. '#' starts a comment.

# ---------------------------------------------------------------------------
# Redraft Leagues — auction, mocks, and the custom scoring editor.
# Flag-gated behind NEXT_PUBLIC_FLAG_* and not reachable in the shipped app.
# Verified 2026-08-26: none of these alter lists, list_players, players,
# player_stats or profiles — no ALTER TABLE, index, trigger, policy or grant
# against them, and none of the 60 functions they define is a launch-surface
# function. One qualification (R659): 083 adds FK references to players(id) and
# profiles(id) from two NEW tables, so once pushed they will constrain DELETEs on
# players — which is sync-script-owned. Neither table's own definition changes.
# These go up when the invite-only test cohort starts.
#
# 105 added 2026-08-26 with SE.5 (the scoring fork/save RPCs). This is the
# mechanism working as designed, not an exception: 105 fell OUTSIDE the closed
# range and would have reddened the check on merge, so holding it back is an
# explicit act recorded here rather than an inherited default.
#
# 106 added 2026-08-31 with SC.1 (the Scout Scoring template seed — spec
# v2.16.9 App B.5). Same mechanism, same explicit act: a seed-only migration,
# no schema change, held with the rest of the leagues chain until the
# invite-only test cohort starts.
#
# 107 added 2026-08-31 with SC.2 (the Scout default roster — WR 2 → 3 in
# leagues.roster_settings' column DEFAULT; spec v2.16.9 §7.3.2). Same
# mechanism, same explicit act: one DEFAULT swap, future rows only, no
# backfill, held with the rest of the leagues chain.
#
# 108 added 2026-09-01 with SC.4 (the Scout pair — the 'Scout Scoring' →
# 'Scout Standard' rename UPDATE + the Scout PPR seed, receptions 0.2; spec
# v2.16.11 App B.5.1 as marked up). Same mechanism, same explicit act: a
# seed/rename-only migration, no schema change, held with the rest of the
# leagues chain until the invite-only test cohort starts.
#
# 109 added 2026-09-02 with L.D1.1 (the M4 in-season tables — `matchups`,
# `team_week_results`, `transactions`, `league_player_pool`, the
# `score_fanout` queue, `player_stats.advanced` (C59) and
# `idx_league_rosters_player`; spec v2.16.11 §12.8/§12.9/§12.18/§12.19/
# §22.4/§23.5). Same mechanism, same explicit act. One qualification in the
# R659 shape: 109 ADDS a DEFAULTed JSONB column + a CHECK to `player_stats`
# (launch-surface, sync-script-owned — additive, every existing row reads
# '{}'), an index on `league_rosters(player_id)`, and two more FK references
# to players(id) (`league_player_pool`, `score_fanout` — the latter ON
# DELETE CASCADE, the former RESTRICT like 083's), so once pushed they
# further constrain DELETEs on players. Nothing else on a launch table.
#
# 110 added 2026-09-02 with L.D1.2 (the schedule engine — `league_generate_
# schedule` + its pure core, the `league_weeks` transition guard (F4), the
# composite week FKs on `matchups`/`team_week_results` (F213), and the
# DROP+CREATE of `draft_complete_internal` / `draft_start_internal` (+ p_now)
# plus the F130/F143 one-hunk replacements; spec v2.16.12 §11.7 Mid-season
# entry). Same mechanism, same explicit act. No launch-surface table is
# touched: every object is on the leagues chain (`leagues`, `league_weeks`,
# `matchups`, `team_week_results`, `drafts`-family functions); the only
# reads of `nfl_weeks`/`nfl_games` are SELECTs inside the engine.
#
# 111 added 2026-09-02 with L.D1.3 (Remix + manual matchup edit — the
# `schedule_actions` idempotency ledger, `schedule_preview`,
# `schedule_remix_confirm`, `schedule_edit_matchup` + two plain helpers; spec
# v2.16.15 §11.7 Remix / E41; D290 interim audit posture). Same mechanism,
# same explicit act. No launch-surface table is touched: the new table
# references `leagues`/`profiles` by FK only (no ALTER on profiles), every
# function is on the leagues chain, and the only reads outside it are SELECTs
# on `nfl_games`/`nfl_weeks`/`teams`.
082-111
